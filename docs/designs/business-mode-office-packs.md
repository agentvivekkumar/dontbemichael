# Design: Business Mode + Office Packs, the hive for solo business owners

Generated by /office-hours on 2026-09-15
Branch: main
Repo: agentvivekkumar/dontbemichael
Status: APPROVED
Mode: Startup

## Problem Statement

Dont Be Michael (forked from Munder Difflin) runs an office of AI agents on the owner's machine. Today it is a developer tool. Onboarding asks for a harness folder, a CLI engine and model ID, and git repos. The Add Agent form edits raw spawn commands, working directories and worktrees. The workspace shows terminals, a Monaco IDE and git panels. A non-technical owner can't get past setup, and the "non-technical" persona only changes the wording (`OnboardingWizard.tsx`, `descPlain` copy keys).

The fork keeps Claude Code as the hidden engine and keeps the hive (mailboxes, router, ledger, breaker, memory, schedules). It replaces everything the owner sees with a business-first flow: pick a business type, pick a starter team of specialized agents, connect the accounts those agents need inside the product, and run the office from the desktop app.

Business model: the app stays open source (MIT). A separate Pro plan (support, training, consulting, dedicated support) is sold on the website and is not built into the app.

## Demand Evidence

- The founder runs an "AI for small business" program for the Greater Austin Asian Chamber of Commerce: "there is a genuine demand by small businesses to leverage AI to do their work as most of them are single person run businesses."
- This is a real distribution channel: a room of owners the founder already reaches.
- **Gap:** no individual member has been named yet (asked twice). The evidence so far is program-level interest, not one owner's behavior. The Assignment closes this gap.

## Status Quo

In the founder's words: "some are pure chatgpt people or worse using chatgpt like google, some know a bit of tools to use to do some part of their business, some knows claude code but not technical enough to run end to end automation like this project can do."

- **ChatGPT-as-search users:** too far from this product for v1 (they would need a Claude plan, an install, and trust in agents all at once).
- **Partial tool users:** stitch together separate apps by hand.
- **Claude Code-aware but stuck:** already pay for the engine and have hit the automation wall. This is the wedge.

## Target User & Narrowest Wedge

- **User:** a solo restaurant or food owner in the chamber program who already uses Claude or Claude Code but can't wire end-to-end automation.
- **First job that broke:** social posts and daily specials. Low risk, visible every day, easy to approve.
- **Wedge:** the **Restaurant & Food Office Pack**. Michael (orchestrator, always on) plus a recommended cast the owner picks from:
  - Oscar (finance, part of every pack);
  - Pam (admin, email triage);
  - Kelly (customer care, inquiries and complaints);
  - Ryan (marketing, promotions and specials);
  - Creed (quality, checklists).
  - Each pick opens its connector setup.

## Constraints

- **Engine:** the Claude Code CLI; owners are expected to be on Claude Max. The hive drives interactive CLI sessions. `src/main/hiddenClaude.ts` deliberately avoids `claude -p` so usage draws on plan quota rather than the separate Agent SDK credit pool (per its code comment). Plan limits therefore apply to the whole office.
- **Delivery:** every provider in `src/shared/agentProvider.ts` uses `inboxDelivery: 'terminal'`. The harness types each inbound message into the agent's live CLI session. Business mode hides terminals but still depends on this path. See Open Question 5.
- **Always-on host:** agents run only while the desktop app is open and the machine is awake.
- **No OAuth support today:** `src/shared/integrations.ts:262-264` leaves Gmail, Calendar and Salesforce out because there is no `oauth2` auth type, and OAuth refresh was a v1 non-goal for upstream. Every restaurant connector (email, Google Business Profile, Instagram/Facebook, QuickBooks) needs OAuth, so this design adds an OAuth broker (Recommended Approach §4). This is the largest new build.
- **Existing secret handling to build on:**
  - Integration secrets are encrypted at rest with Electron `safeStorage` (`src/main/integrations.ts:111-132`), which refuses to store a secret when OS encryption is unavailable.
  - The loopback broker (`src/main/integrationBroker.ts`) injects the credential upstream, so an agent never sees it.
- **Per-agent enforcement exists:**
  - `control.ts:107` `toolDecision` denies gated tools per agent through the PreToolUse hook.
  - Per-session settings files carry `mcpServers`, sandbox and hooks and never write `~/.claude` (`hive.ts:1146`).
  - The only global writes are the skip-permission-prompt flags (`config.ts:786-858`).
- **Windows has no sandbox:** the OS sandbox block in `hookSettings` runs without isolation on Windows.
- **Licensing:** code is MIT, and the LimeZu pixel art needs credit (`LICENSE-ASSETS`).
- **Upstream:** keep bug fixes flowing from Munder Difflin where practical.

## Premises

Agreed after the founder revised #2 through #6, then refined at the wireframe review:

1. **Wedge user:** chamber members who already use Claude or Claude Code but can't run end-to-end automation.
2. **Starter team:** business type leads to Michael plus a recommended cast.
   - The owner picks any subset as a starter template, and each pick opens that agent's default config and connector setup (picking Pam asks to connect email).
   - **Oscar (finance) is recommended in every pack, because every business needs finance.**
3. **Desktop control room:** the owner monitors, approves and rejects in the Mac/Windows app. Agents run autonomously and earn more autonomy as they learn.
4. **Agent Definition v2:** role, what to do, what not to do, and a per-agent tool allowlist (for example, the marketing agent can't read email). The owner refines each agent's defaults. **"Won't" rules are owner-editable starting defaults, not fixed limits: an agent can do anything the owner turns on.**
5. **Connectors in the product:** connectors are configured in the product UI, reusing the engine's connector mechanism (MCP) underneath. The owner never touches Claude Code setup.
6. **Plan and schedule:** owners are expected to be on Claude Max. The product lets the owner choose the model and set an office schedule (for example, pause during lunch). Kept after an outside challenge.

## Cross-Model Perspective

Outside voice: local ollama `gpt-oss:20b`, cold read of a structured summary. Every claim was checked against the source.

- **Steelman:** "a lightweight, desktop-first AI-powered office that lets non-technical small-business owners pick a role-based agent cast, automatically wire the necessary connectors, and let the agents autonomously execute routine tasks while the owner monitors and can intervene via a simple approval UI."
- **Key signal it quoted:** "not to solve one part of the problem of a business but using multiple agents specialized for each part of the business."
- **Challenged premise #6 (Claude Max):** cost blocks owners on ChatGPT or free tiers.
  - *Half valid:* the cost point is real, and the app supports 12 engines.
  - *Wrong:* its claim of an "open-source Claude Code SDK" swap.
  - The founder kept #6 because the wedge is people already paying for Claude.
- **48-hour prototype:** a business-type wizard, a role cast, a Gmail connection, an approval panel.
  - *Wrong 1:* it called the tool gating an "MCP hook". Enforcement is actually `control.ts` through the PreToolUse hook.
  - *Wrong 2:* Gmail OAuth is not a 48-hour item, because OAuth is unsupported (`integrations.ts:262`).
- **Survival:** 2 of 4 points intact, 2 needed factual corrections.

## Approaches Considered

### Approach A: Business Mode in the existing app (chosen)

- Keeps the hive untouched.
- Adds the business onboarding, the Agent Definition v2 schema, the OAuth broker, a connector center, an approvals inbox and an office schedule.
- Hides developer surfaces in business mode.
- Effort L (human ~2 months / CC ~2-3 weeks).
- **Risk medium-high.** The drivers:
  - the OAuth broker, which is net-new security-sensitive code;
  - third-party app approvals (Meta, Google), whose timelines we don't control;
  - developer UI leaking through a missed mode check.
- The build order below puts the connector-free work first, so the pilot isn't blocked on those approvals.

### Approach B: Business Edition on an extracted hive core

- Splits the code into `hive-core` with an Engine adapter, plus a new owner app.
- Cleanest long term. Effort XL (human ~5 months / CC ~6-8 weeks).
- **Not chosen now:** months before any chamber owner uses it, and extracting from `hive.ts` (3.5k lines mixing CLI flags, git and routing) is risky. A's schema, broker and pack format are what B would extract later.

### Approach C: Office Packs + chamber concierge

- Portable pack files authored from real member setups, with a thin importer app.
- Effort M.
- **Not chosen alone:** too thin for owners who download on their own. **Its pack format is adopted as A's template layer.**

## Recommended Approach

Approach A, with Office Packs as the template format.

### 1. Office Pack format (`dontbemichael/office-pack@1`)

A portable JSON file that extends the existing `hire@1` manifest validator (`src/shared/hire.ts`) rather than replacing it.

- **Core pack:** `resources/packs/core.json` holds the agents every business needs.
  - v1 contains only Oscar (finance).
  - Every business pack inherits the core agents. A pack may tune a core agent's `does` / `wontDo` / `tools` for its industry (restaurant Oscar watches food cost and supplier invoices) but can't remove it. The validator enforces this, so community packs can't leave out finance.
  - The owner can still leave Oscar unpicked.
- **Import:** through the existing file and deep-link path (`index.ts:2170-2240`). An import only pre-fills the form; the owner still confirms.
- **Distribution:** bundled packs ship in `resources/packs/`. Community packs are contributed by PR.
- **v1 packs:** Restaurant & Food only, plus a generic "Something Else" pack Michael fills in by asking questions. Retail, Pro Services, Salon and Home Services follow once the restaurant pack survives real owners.

Shape, for orientation only (the implementation plan fixes names and validation):

```ts
interface OfficePack {
  spec: 'dontbemichael/office-pack@1';
  businessType: string;            // 'restaurant-food'; 'core' for the core pack
  displayName: string;             // 'Restaurant & Food'
  tagline: string;                 // 'cafe, catering, food truck'
  extends?: 'core';                // every business pack; the core pack omits it
  agents: AgentDefinitionV2[];     // business roles, plus overrides of core agents by id
  defaultPicks: string[];          // agent ids pre-checked on the team screen (core agents included)
  officeHours: {
    days: ('mon'|'tue'|'wed'|'thu'|'fri'|'sat'|'sun')[];
    work: { start: string; end: string };        // '09:00'..'22:00', owner's local time
    pauses: { start: string; end: string; label: string }[];
  };
  starterMissions?: { agentId: string; title: string; schedule: string }[];
}
// Michael is always on and never listed in a pack.
```

### 2. Agent Definition v2

```ts
type ToolLevel = 'off' | 'ask' | 'auto';

interface AgentDefinitionV2 {
  id: string;                      // 'pam'
  character: OfficeCharacterName;  // cast.ts
  role: string;                    // 'Admin'
  summary: string;                 // one plain sentence shown on the team card
  does: string[];                  // plain-language starting rules, owner-editable
  wontDo: string[];                // starting defaults, owner-editable; not shown on team cards
  connectors: { kind: string; required: boolean }[];   // 'email', 'google-business', 'meta', 'quickbooks'
  tools: { capability: string; level: ToolLevel }[];   // 'email.read', 'email.send', 'social.post'
  modelTier?: 'best' | 'fast';     // mapped to a concrete model id in main, never shown
  tokenCap?: number;
}
```

Enforcement follows whatever the owner has set. Layers, strongest first:

1. **The agent is never given an MCP server it isn't allowed to use.** Per-session `mcpServers` includes only its connectors, and within a connector only the capabilities not set to `off`.
2. **`ask`-level capabilities route through `control.ts` gating** into the approvals queue.
3. **`off`-level tools are gated** (and passed as `--disallowedTools` where applicable).
4. **`does` / `wontDo` compile into the appended system prompt** as reinforcement only, never as the enforcement layer.

If the owner turns something on (for example Pam sending email on her own), every layer updates to match. Nothing is hard-coded as off.

### 3. Business onboarding (replaces the dev wizard steps when the audience is business)

1. **Business name, city and business type.** The Claude install and sign-in check runs silently.
2. **Starter team picker.**
   - Michael is always on.
   - Core agents (Oscar) and recommended agents come pre-checked.
   - "Needs" chips show each pick's setup cost.
   - No "won't" tags on these cards.
   - Office hours are set here.
3. **Per-agent setup, in pick order:**
   - connect account (OAuth broker);
   - edit the job in plain words ("does" and "won't" are starting rules);
   - set tool levels.
4. **Land on the floor** with the approvals inbox open.

- Engine is fixed to Claude Code, and the model is chosen by friendly tier (`best` / `fast`) in Settings.
- The harness folder is created silently.

**Failure and empty paths.** Each has one plain sentence and one next action:

| Situation | What the owner sees | Next action |
|---|---|---|
| Claude not installed | "Claude isn't on this computer yet." | [Install Claude] runs the existing `cliInstall.ts` flow with no visible terminal |
| Claude not signed in | "Sign in to Claude. A Max plan is recommended." | [Sign in] opens login in a hidden session. The app has no verified way to read the plan tier, so it recommends Max rather than checking it; limits surface through §7 |
| OAuth window closed or cancelled | "Google sign-in was closed before it finished." | [Try again] |
| Wrong account (e.g. personal Gmail where the pack expects the business inbox) | Shows the signed-in address | [Use this account] / [Switch account] |
| Provider denies a permission scope | "Pam needs permission to read email to sort it." | [Try again]; otherwise the agent stays paused |
| Owner clicks "Skip for now" | Agent saved as **paused · waiting for account** | Card on the floor: [Connect] |
| Token later expires or is revoked | That agent pauses; approvals inbox card "Reconnect Google" | [Reconnect]; the other agents keep working |
| "Something Else" business type | Michael asks 3 plain questions (what you sell, who contacts you, what slips) | Builds a team from the core pack plus the generic pack; never a dead end |
| Owner wants roles from another pack later (e.g. a restaurant adds a retail counter) | Settings → "Hire from another pack" | Adds agents alongside the current team; existing agents, memory and connectors stay; the wizard is not re-run |

- Wireframe (revision 2): `docs/designs/business-mode-office-packs-wireframe.png`, with its source at `docs/designs/business-mode-office-packs-wireframe.html`. It uses the real `DESIGN.md` tokens; portraits are placeholders in each character's shirt color.

### 4. Connector Center + OAuth broker

A main-process OAuth 2.0 broker:

- System browser with a loopback redirect and PKCE (the sign-in pattern for apps that can't keep a secret).
- Refresh tokens stored through the existing secret store (encrypted at rest with Electron `safeStorage`).
- **Access tokens refreshed inside the loopback broker (`integrationBroker.ts`) before it injects them upstream**, so agents keep never seeing a credential.
- Connectors exposed to agents only through per-session MCP servers or the broker proxy.

**v1 provider order is decided by the Assignment results.** The expected shortlist is Google (Gmail, Business Profile), Meta (Instagram/Facebook), Microsoft 365, and QuickBooks for Oscar. An "Other email" IMAP path is the fallback.

### 5. Business shell

In business mode, hide the terminal panes, IDE, git panels, worktrees, CI/GitHub view, skills catalog, raw spawn/flags fields, the Prerequisites developer list, and model IDs.

Keep:

- **Floor:** the office floor, as the ambient view.
- **Agent cards:** each card shows the agent's job in plain words.
- **Approvals inbox:** approve, reject, or edit and approve.
- **Activity feed:** written in plain language.
- **Office schedule.**
- **Connector Center.**

Leak protection: a test renders business mode and fails on any developer term (`terminal`, `worktree`, `git`, `repo`, `CLI`, model IDs).

### 6. Office schedule

Work window plus pause windows, reusing `weeklySchedule.ts` for day-of-week rules. A pause maps to the existing `control.ts` pause for every agent and resumes at the window end. Work that arrives during a pause (e.g. an inbound email) is queued, not dropped. The manual "Closing time" (`closingTime.ts`) stays as the owner's big red button.

### 7. Plan limits

When the Claude plan's usage limit is reached, every agent on that plan stops getting responses until the window resets.

- **Office-wide event:** the limit is treated as an office-wide event, not as an agent error, and must never trip the circuit breaker as a "loop".
- **Detection:** how the app reliably detects the limit is unverified (Open Question 10). Spike it before building this section.
- **What the owner sees:** the floor shows "Office on break until 3:10 pm (Claude plan limit)". Michael holds new work in the queue, and a notification fires once, not once per agent.
- **Resume:** agents resume automatically at the reset time.
- **Settings:** a plain usage meter, built on the existing usage and cost tracking in `src/main/usage.ts` and `costLifetime.ts`, shows how close the office came to its limit this week. The owner can move a busy agent to the `fast` model tier.

### Build order (v1 cut)

1. **Phase 1, no third-party connectors:**
   - business shell and leak test;
   - onboarding screens 1-2 plus Claude checks;
   - Agent Definition v2 schema and enforcement compiler;
   - core pack and Restaurant Office Pack;
   - office schedule and plan-limit handling;
   - approvals inbox.
   - Runnable agents:
     - Michael.
     - Creed (checklists, no connector).
     - **Ryan in draft mode.** Ryan writes daily specials and promotions into the approvals inbox, and the owner copies them to Instagram or Facebook by hand. This serves the first broken job before any Meta approval exists.
     - **Oscar in manual mode.** The owner drops bills, receipts and daily sales totals onto Oscar's desk (files in the office folder, added from the app). Oscar keeps the running tally, flags what's due, and sends a weekly money summary. The file drop is new UI.
2. **Phase 2:** OAuth broker plus the first provider picked from the Assignment (expected: Google, which unlocks Pam's email triage, Kelly's inquiry replies, and bills reaching Oscar through Pam).
3. **Phase 3:**
   - Meta connector (Ryan auto-posting), after Meta app review;
   - Google Business Profile reviews (Kelly), after API access approval;
   - QuickBooks (Oscar), if the Assignment shows members use it;
   - further packs.

## Open Questions

1. **No named member yet.** Who are the first three restaurant or food members, and which starter agents would they actually pick?
2. **Owner away from the desk.** The founder chose the desktop as the control room. A restaurant owner is on the floor 10 hours a day, so how long do approvals wait? Measure approval latency in the pilot before deciding whether phone approvals are needed.
3. **Permission model.** Can business mode run unattended without `--permission-mode bypassPermissions` and the global skip-prompt flags, by granting exactly the allowlisted tools through per-session permission rules? Needs a spike on the current Claude Code version.
4. **Automated use of a consumer plan.** Verify Anthropic's current terms for automated, all-day business use of a Claude Max subscription driven through interactive sessions before promoting this to chamber members.
5. **Terminal-typed delivery reliability** when no human ever looks at the session. Today `'terminal'` delivery types messages into the live CLI and relies on the Stop hook to drain the inbox. `'serve'` is a declared but unimplemented mode where the harness would hand messages to a headless server interface instead. Is `'serve'` needed before v1, or is terminal delivery reliable enough with nobody watching?
6. **Third-party API approvals.** Expect review or verification gates for Instagram content publishing (Meta), the Google Business Profile API, and QuickBooks app listing (Intuit). Confirm the timelines early, since they gate Phase 3.
7. **Character names.** The Office names (Michael, Oscar, Pam, Kelly, Ryan, Creed) are carried over from upstream. With a paid Pro offering attached, is there trademark exposure? Decide whether packs ship with the cast names or neutral role names with cast skins.
8. **Windows.** There is no OS sandbox for agent subprocesses. Is that acceptable for business users with real customer and financial data, or does business mode ship macOS-first?
9. **Upstream sync.** How far can business mode diverge before merging Munder Difflin fixes stops being practical?
10. **Plan-limit signal.** What does Claude Code actually emit when the plan limit is hit: message text, a hook event, a transcript entry? Parsing free text is fragile. Find a dependable signal in a spike before building §7.
11. **Money actions.** The owner can turn anything on, so what does "on its own" mean for Oscar paying a bill? Decide whether money-moving capabilities exist in v1 at all or arrive later with extra confirmation.

## Success Criteria

- **Unassisted setup:** 3 chamber restaurant or food owners each go from install to a first running agent in 20 minutes or less, with the founder watching and not helping. Phase 1 counts Creed, Ryan in draft mode, or Oscar in manual mode; Phase 2 adds a connected Pam.
- **Retention:** at 14 days, at least 2 of those 3 owners still have at least one agent running, and each has approved at least 5 agent actions.
- **Enforced tool limits:** automated test proves a marketing agent (Ryan) with `email.read: off` cannot read email, whatever its prompt says, and that switching it to `auto` grants access.
- **Core pack enforced:** pack validator rejects a business pack that drops Oscar.
- **No developer UI in business mode:** the leak test passes in CI.
- **Plan limits are visible:** a simulated plan-limit event shows the office-wide break message with a resume time, fires one notification, and does not trip the circuit breaker.
- **CI runs the test suite:** CI runs `npm run test:focused` (today it only runs typecheck, the link check and the build).

## Distribution Plan

- **Same app:** Business Mode ships in the existing electron-builder pipeline (`npm run dist:mac` / `dist:win`), published through the current releases page and dontbemichael.com downloads.
- **Office Packs:**
  - Bundled in `resources/packs/`.
  - Importable as a file or a deep link.
  - Community packs by PR to a `packs/` folder with schema and core-pack validation in CI.
- **Pro plan:** a website offer (workshops, setup, dedicated support, and custom packs authored as a service). Custom packs use the same pack format and are not an app feature.
- **Rename:** remaining `munder-difflin` names (`package.json`, i18n copy, `DESIGN.md`, deep-link scheme) before chamber distribution, so owners see one brand.

## Dependencies

- Agent Definition v2 schema and compiler into per-session settings and gating (Phase 1).
- Office Pack schema, core pack and validator (Phase 1).
- Business-mode shell flag across the renderer (Phase 1).
- Oscar's file drop (Phase 1).
- Claude Code installed and signed in on the owner's machine.
- OAuth broker (Phase 2; blocks Pam and Kelly).
- Meta, Google and Intuit developer app setup and approvals (Phase 3; start the paperwork in Phase 1).
- CI: add the test run.

## The Assignment

Before building the OAuth broker, sit with **three named chamber restaurant or food members** (initials are fine) for 30 minutes each.

1. Show them the Screen 2 wireframe (starter team) and ask them to pick.
2. Write down:
   - which agents they pick and which they skip, and why, in their words;
   - which accounts they would have to connect (Gmail or Outlook, Instagram, Facebook, Google Business Profile, POS, QuickBooks or other bookkeeping);
   - whether they are on a Claude plan today;
   - the moment they say "I wouldn't do that" or stop.
3. Bring the three sheets back.

The Phase 2 provider, Oscar's first connector, the default starting rules and the fate of premise #6 come from those sheets, not from this doc.

## What I noticed about how you think

- You corrected the frame the moment it got too narrow: "the idea is not to solve one part of the problem of a business but using multiple agents specialized for each part of the business." That is a product thesis, not a feature request.
- On premise #4 you went straight to least privilege with a concrete case: "disallowing a marketing agent to look into emails and stay within." At the wireframe you drew the other half of that line too: "agents would be able to if business owner wants that." Limits are the owner's tool, not the product's opinion.
- You spotted what every template was missing: "finance is required no matter what is the business." That one line turned a set of industry templates into a core-plus-industry model.
- The one soft spot: "restaurant owner" and "social posts" came without a name attached, twice. Your chamber access is the strongest card here, and the Assignment is about playing it.

## Reviewer Concerns

Spec review ran 2 rounds on local ollama `gpt-oss:20b`. One issue repeated after its fix and was stopped by the convergence rule:

- **"'No OAuth support today' contradicts the OAuth broker."** Not changed. The constraint describes the codebase before this design, and the same bullet says this design adds the broker. Recorded so a later reviewer can judge the wording.

## Engineering Review Decisions

From /plan-eng-review on 2026-09-15, scope: all three phases. Each decision was approved individually.

### Architecture

**1. Closing the window must not silently stop the office** (P1, confidence 9/10)

- **Finding:**
  - On Windows, closing the last window runs `teardownAndQuit()` (`src/main/index.ts:5374-5380`), which kills every agent.
  - On macOS, messages reach agents through a 4-second timer inside the window (`src/renderer/src/hooks/useHive.ts:690-692`), and the main-process fallback skips Michael (`index.ts:5146`).
  - Open Question 5 is also wrong: the Stop hook never drains the inbox (`src/main/hooks.ts:261-264`).
- **Decision:** closing the window asks the owner to choose:
  1. **Minimize:** hide to a tray icon and keep the office running.
  2. **Close all agents gracefully:** run the Closing Time shutdown (`closingTime.ts`), wait a bounded time, then quit.
  3. **Force kill all:** immediate `teardownAndQuit()`.

  Built on the existing `app:closeRequested` flow (`index.ts:5361`), so the red close button and Cmd-Q / dock-quit show the same dialog.
- **Required for Minimize to work:**
  - The window is hidden, not destroyed, so renderer timers keep running (`backgroundThrottling: false` is already set at `index.ts:2310`).
  - Windows no longer tears down on window close.
  - The main-process wake fallback includes Michael.
- **Tests:**
  - win32 window close does not tear down while Minimize is chosen.
  - A graceful close times out into a kill.
  - The main wake fallback delivers to Michael with no renderer.

**2. Capability limits are enforced by the broker, the only component holding credentials** (P1, confidence 9/10)

- **Finding:** §2 enforcement layer 1 relies on MCP server omission plus tool-name gating. But agents reach integrations with plain HTTP from Bash, using a broker token in their environment:
  - the token covers every enabled integration (`src/main/index.ts:4735-4737`);
  - the hook matches exact tool names, so it only ever sees `Bash` (`src/main/control.ts:111`);
  - Claude Code's docs say Bash rules are "not a security boundary around the program" (permissions.md:247).
- **Failure scenario:** Pam's Send is Off, yet `curl` to the broker's Gmail send endpoint still sends the email.
- **Decision:**
  - **Per-agent grants:** an agent's broker token covers only that agent's connectors.
  - **Capability map per connector:** each request's method and path pattern maps to a capability (e.g. Gmail send → `email.send`). The broker checks it against the agent's current `off | ask | auto` level on every request. **Unmapped endpoints are denied by default.**
  - **Live levels:** the broker reads levels from the persisted Agent Definition, so a change applies without respawning the agent.
  - **OAuth scopes follow levels:** a scope is requested only when a capability that needs it is above Off. Raising a level triggers incremental authorization.
  - **Agent-facing tools:** agents use connectors through thin MCP tools that call the broker. Tool-name gating in the hook stays as a second layer.
  - **§2 enforcement order becomes:**
    1. broker capability check;
    2. per-session MCP servers only for the agent's connectors;
    3. PreToolUse gating;
    4. prompt reinforcement.
- **Tests:**
  - capability map matching, including unmapped-endpoint default deny;
  - grant scoping (Ryan's token rejected on Gmail);
  - a level change applies to the next request without respawn;
  - an incremental scope request fires when Send moves above Off.

**3. "Ask me" uses a durable approvals queue; the broker runs the approved request** (P1, confidence 9/10)

- **Finding:** §2 sends `ask`-level calls "into the approvals queue", but no queue exists:
  - `src/main/hooks.ts:272-273` leaves approval to Claude's native prompt.
  - Michael's prompt says "there is NO separate approval queue" (`src/main/hive.ts:1487`).
  - `control:approvalRequest` (`hooks.ts:381`) has no listener in the renderer.
  - A call can't be held while the owner is busy: the hook shim exits after 5 s (`hive.ts:3001`), and Claude Code's command-hook timeout is 600 s.
- **Decision:**
  - **Record:** each approval is persisted in the hive folder as `approvals/<id>.json` with agent, capability, plain-language summary, exact request payload, created and expires times, status, and owner note.
  - **Connector requests at `ask`:** the broker stores the record and immediately returns "queued as approval #id" to the agent.
  - **Approve:** the broker executes the stored request itself, so there is no agent retry and no payload drift, and it delivers the result to the agent's inbox. **Edit and approve** executes the edited payload.
  - **Reject and expiry:** expiry defaults to 24 h (pack-configurable). Both deliver an inbox message with the owner's note or "expired".
  - **Non-connector tools at `ask`** (e.g. deleting a file): PreToolUse denies with the approval id. Approving issues a one-time grant for that exact call plus a "retry now" inbox message.
  - **Duplicates:** pending requests with the same agent, capability and payload hash merge into one approval.
  - **Breaker:** denials caused by a pending approval don't count toward the error storm.
  - **Reuse:** the ASK ME board's answer-lands-in-inbox pattern. In business mode, Michael's "no approval queue" prompt line is rewritten.
  - **Ryan's draft mode:** an approval item whose approve action is "copy to clipboard and mark posted".
- **Tests:**
  - persists across restart;
  - approve executes the exact stored payload exactly once (double-click safe);
  - edit and approve;
  - reject and expiry inbox messages;
  - dedup;
  - one-time grant is consumed;
  - pending denials don't trip the breaker.

**4. Business agents run in `dontAsk` with compiled rules and a default-deny hook** (P1, confidence 9/10)

- **Finding:**
  - `--permission-mode bypassPermissions` is on by default (`src/shared/agentProvider.ts:176`, `src/main/config.ts:427`).
  - Every Claude spawn writes skip-prompt flags to `~/.claude/settings.json` **and** folder trust to `~/.claude.json` (`config.ts:796-797`, `:823`; `index.ts:2928-2930`). The Constraints line "the only global writes are the skip-permission-prompt flags" is therefore incomplete.
  - The hook shim exits silently on a socket error or after 5 s (`hive.ts:2999`, `:3001`). Claude Code treats that as normal permission flow, and with `bypassPermissions` that means allow.
  - Gated tools live only in memory.
- **Decision:**
  - **Permission mode:** business agents spawn with `--permission-mode dontAsk`, which "auto-denies every tool call that would otherwise prompt" (Claude Code permission-modes docs).
  - **Compiled per-session rules** from the Agent Definition:
    - `permissions.allow`: only the built-in tools and Bash patterns the role needs, scoped to the agent's office folder, plus `mcp__<its connector servers>__*`;
    - `permissions.deny`: high-risk built-ins the agent isn't granted (e.g. WebFetch, network Bash).
  - **Default-deny hook:** PreToolUse returns an explicit allow for listed tools and deny for everything else. It evaluates the persisted Agent Definition, so decisions survive restarts. It also blocks widening from the owner's personal `~/.claude` allow rules whenever the hook is reachable.
  - **Global writes:** business mode no longer writes skip-prompt flags to `~/.claude/settings.json`, and the `~/.claude.json` trust write is limited to office folders.
  - Developer mode is unchanged.
- **Spike S2 (before building)**, on the current Claude Code version:
  - an unattended `dontAsk` session in a PTY;
  - trust-dialog behavior without the skip flags;
  - loading only the agent's MCP servers (e.g. `--mcp-config`) so personal servers don't load;
  - the residual risk while the hook is unreachable (personal allow rules for built-ins still apply).
- **Tests:**
  - rule compiler output per role;
  - hook allowlist decisions, including unknown tools;
  - decisions survive restart;
  - no writes to `~/.claude/settings.json` in business mode.

**5. Plan limits trigger a hook-driven office break** (P2, confidence 8/10)

- **Finding:**
  - §7 leaves detection unverified, and nothing in `src/` handles limit text or quota events.
  - Per-session settings register Stop, SubagentStop, PreToolUse, PostToolUse, UserPromptSubmit, Notification, SessionStart, PreCompact and PostCompact, but **not StopFailure** (`src/main/hive.ts:1195-1205`).
  - The breaker's error storm (5 errors, `breaker.ts:93`) is fed by OTel `api_error` events (`index.ts:277`). A limit stall can therefore constrain agents and fill their inboxes with breaker mail.
- **Decision:**
  - **Hooks:** register `StopFailure` with matcher `rate_limit`, and handle the Notification types `quota_auto_resume_fired`, `quota_auto_resume_stale` and `quota_auto_resume_disabled` (Notification is already registered).
  - **Office on break:** the first limit event sets a single office-wide state in main.
    - Auto-delivery pauses for every agent.
    - Mission fires are deferred.
    - The breaker ignores rate-limit errors while the office is on break.
    - The owner gets one notification, and the floor shows "Office on break until <time> (Claude plan limit)".
  - **Resume:** on `quota_auto_resume_fired` or the payload's reset time. If neither is present, retry after a capped backoff timer.
  - **Stagger:** resumed deliveries are spread out so agents don't all start at the same moment.
- **Spike S3:** on a Max account, record real `StopFailure` and quota Notification payloads: their fields, the reset time, and whether interactive sessions auto-resume. Keep them as test fixtures.
- **Tests:**
  - replayed fixtures enter the break state;
  - the breaker is untouched;
  - exactly one notification fires;
  - resume works on the auto-resume event and on the timer fallback;
  - resumed deliveries are staggered.

**6. Office hours use a restart-safe soft pause** (P2, confidence 8/10)

- **Finding:** §6 maps a pause to the `control.ts` pause, which has three problems:
  - It denies every tool call ("Paused by operator", `src/main/control.ts:110`) rather than stopping new work.
  - Message delivery checks a different flag, `autoDeliveryPaused` (`src/renderer/src/hooks/useHive.ts:818`).
  - Only `autoDeliveryPausedAgents` is persisted (`src/main/index.ts:3943-3948`), and mission timers in `syncMissions` fire regardless.
- **Decision:**
  - **Computed state:** working, paused or closed comes from `officeHours` plus the current time. There is no stored "paused" flag, so a restart can't lose it.
  - **At pause start:** auto-delivery pauses for every agent through the existing persisted flag. In-flight turns finish, and no tools are denied.
  - **Missions and inbound work:** a mission due during a pause fires once at window end (coalesced per mission). Inbound work, like email sorted by Pam or webhooks, still lands in inboxes and waits.
  - **Resume:** deliveries are staggered, sharing the stagger from Decision 5.
  - **Owner override:** "Pause office now" and "Resume now" carry an expiry, by default until the next scheduled transition.
  - **Combined with Decision 5:** the office works only when both the schedule and the plan-limit state allow it.
  - The §6 sentence "A pause maps to the existing `control.ts` pause" is superseded.
- **Tests:**
  - transitions at window edges;
  - overnight windows;
  - DST change days;
  - restart mid-pause stays paused;
  - mission coalescing;
  - override expiry;
  - schedule combined with the plan-limit break.

**7. Imported packs are capped at "Ask me"** (P2, confidence 9/10)

- **Finding:** §1 says "An import only pre-fills the form; the owner still confirms". Packs raise the stakes over today's hire manifests because they carry tool levels and plain-language rules. Today:
  - `consentRequired` is computed (`src/shared/hire.ts:302`, returned at `:315`) but nothing in `src/` or `test/` reads it.
  - Unknown fields are silently dropped (the output is rebuilt field by field at `hire.ts:316`).
  - Deep links accept any https manifest URL (`isAllowedManifestUrl`, `hire.ts:340-345`).
- **Decision:**
  - **Bundled packs** (`resources/packs/`, shipped inside the app) may set any default level.
  - **Imported packs** (deep link or file) are capped at `ask`, at import time, for every capability that leaves the building: send, post, reply, delete, spend.
  - **Raising a level:** only the owner raises a level above `ask`, on the agent's setup screen. The cap is enforced in the pack loader, not only in the UI.
  - **Review screen:** before anything is created, a screen labeled "From outside this app" shows every agent, every `does` / `wontDo` rule, every connector and every requested level.
  - **Validation:** the pack validator rejects unknown fields and invalid levels instead of reusing the hire validator's silent drop.
  - **Links:** keep the existing https-only allowlist.
  - **Pro custom packs:** import as files under the same cap.
- **Tests:**
  - the cap applies to imported packs for each outward capability;
  - bundled packs keep their defaults;
  - the review screen lists every rule and connector;
  - an unknown field is rejected;
  - the cap holds in the loader even if the UI is bypassed.

**8. First mailbox rides the owner's Claude connector; sending and extra mailboxes are product-owned** (P1, confidence 8/10)

- **Finding:** research during this review corrected §4 and premise 5.
  - Agents already inherit the owner's claude.ai connectors, because each agent is a normal Claude session signed in as the owner and the app passes no `--strict-mcp-config`. The app arranged none of it (`hive.ts:1168-1172`).
  - A connector holds **one account per service**, so per-agent mailboxes are impossible on that path.
  - Anthropic's connector docs say of Gmail: "Claude can search your emails but can't send them."
  - Google's API terms §4b(1): "Developer credentials may not be embedded in open source projects."
  - Microsoft: desktop apps are public clients and "can't have client secrets", so only an app ID ships.
  - Meta: "Never include your App Secret in client-side or decompilable code", and native apps are told to use their own server. A project-run server was ruled out.
- **Decision:**
  - **First mailbox:** the owner's existing Claude connector, for **reading and triage only** (Pam). The Connector Center labels it "Connected through your Claude account".
  - **Sending, and every additional mailbox:** product-owned connectors through the broker (Decision 2), one record per account, per-agent grants. Per provider:
    - **Microsoft:** app ID embedded in official builds, injected at build time exactly like `POSTHOG_KEY` (`electron.vite.config.ts:15-19`, `release.yml:56`), never committed.
    - **Google:** the owner's own client through an in-product wizard, until Google's verification team answers in writing whether an embedded desktop client is acceptable.
    - **Meta and other secret-requiring providers:** the owner's own app. Until one exists, Ryan stays in draft mode.
    - **Every provider** also accepts a pasted client ID, so forks and self-builds work.
  - **Generic template:** `IntegrationAuthType` gains `oauth2` (auth URL, token URL, scopes, capability map), so adding an OAuth tool such as a project-management app is data, not code (today it is `none | bearer | header | github`, `src/shared/integrations.ts:29-33`).
  - **Inherited connectors:** agents on product-owned connectors run with inherited connectors off (`ENABLE_CLAUDEAI_MCP_SERVERS=false` per agent). The one exception is the agent using the owner's first-mailbox connector.
- **Spike S4:** Intuit/QuickBooks client model, and Google's written answer on an embedded desktop client.
- **Tests:**
  - `oauth2` template validation;
  - a build with no client IDs falls back to "paste your own";
  - per-agent routing between the connector mailbox and broker mailboxes;
  - a send attempted through the connector path is refused with a plain message.

### Code quality

**9. Connections are central; agents are authorized to use them** (P1, confidence 8/10)

This supersedes the per-agent MCP parts of Decisions 2 and 4.

- **Finding:** Decisions 2 and 4 assumed each agent received only its own MCP servers, written into that agent's settings file (`hive.ts:1168-1172`). That key is undocumented in Claude Code's MCP docs, and per-agent server sets would mean registering the same third-party system twice whenever two agents share one company account.
- **Decision:**
  - **One place to connect:** every external system is configured once in Settings, whether it is OAuth, a plain REST endpoint, or an MCP server. One record per account, with one set of credentials.
  - **Per-agent authorization is about use, not loading:** each Agent Definition lists the connections it may use, and at what level (`off | ask | auto`) per capability. An agent is never given a second copy of a shared connection.
  - **Enforcement stays where it can't be bypassed:**
    - broker requests are checked against the calling agent's grants and capability levels (Decision 2);
    - MCP tools are governed by the agent's compiled allow and deny rules plus the default-deny hook (Decision 4). A shared server means the tool exists in the session, so `dontAsk` plus deny rules is what makes an unauthorized call fail, and deny rules "block in every mode".
  - **Decision 2's enforcement order becomes:** 1) broker capability check; 2) per-agent allow/deny rules and the default-deny hook over shared tools; 3) prompt reinforcement.
- **Known cost:** stdio MCP servers still run one process per session, so a shared connection is shared configuration, not a shared process. Remote MCP and broker connections are shared outright.
- **Tests:**
  - two agents sharing one connection produce one credential record;
  - an agent without authorization is denied at the broker and at the hook;
  - removing an agent's authorization takes effect without respawning it.

**10. Approvals extend the blocked-card / ASK ME flow** (P2, confidence 8/10)

This supersedes Decision 3's separate `approvals/<id>.json` store. The queue already exists; what is missing is a stored action, an executor and result states.

- **Finding, from the working system:**
  - Work blocked on a human is already a task card with `status: 'blocked'` and a `humanQA` entry (`src/main/hive.ts:116`, protocol at `:2859-2870`), rendered on the ASK ME board. Answering patches the card and sends Michael an inbox message: "The answer is also recorded in the card's humanQA. Act on it, unblock the card, and continue the work" (`AskMeTab.tsx:115-130`). Nothing auto-resumes, and no test covers this flow.
  - Webhooks already have a pending-approval state (202 `awaiting-approval`, held tokens in SQLite, `index.ts:1786-1800`).
  - **Gap:** only `done` cards are reported back (`index.ts:2029-2040`); a failed card reports nothing. A failed brokered call returns its HTTP error to the calling agent only (`integrationBroker.ts:154-158`, `:264`). Retry handling is ad hoc (`useHive.ts:788`, `index.ts:1563-1568`), with no shared utility.
- **Decision:**
  - **An approval is a blocked task card carrying an action payload** (connection, capability, plain summary, exact request, expiry). It inherits `tasks.json` durability, the ASK ME board, Michael's protocol and the done-observer.
  - **The business-mode approvals inbox is a friendlier rendering of that same queue**, not a new store.
  - **On approve** the broker executes the stored request (Decision 2), idempotently, so a double tap sends once. **Edit and approve** executes the edited payload.
  - **One terminal state per approval:** executed, failed, expired (default 24 h), rejected, superseded. Each writes a plain-language result to the card and an inbox message to the agent.
  - **Retry:** transient failures are retried once, following the Slack terminal-vs-transient classification already in `index.ts:1563-1568`; a permanent failure stops and asks the owner.
  - **Recovery cases:** a revoked or expired connection invalidates the approval with "Reconnect and approve again"; if the agent is gone the result goes to Michael; during a plan-limit break (Decision 5) approvals wait rather than fail.
  - **Closes the existing gap:** failed cards now report back the same way `done` cards do.
- **Tests** (this flow has none today):
  - blocked-card approval survives restart;
  - approve executes exactly once under a double tap;
  - edit and approve;
  - each terminal state writes card result and agent message;
  - transient retry then permanent stop;
  - revoked connection, stopped agent, and plan-limit break paths.

**11. Business mode ships in all three locales, and CI runs the tests** (P2, confidence 9/10)

- **Finding:** the app ships `en`, `zh-CN` and `ar` with 1203 keys each, and two tests enforce identical key trees and placeholder parity (`test/arabic-ui.test.cjs:189-197`, `test/i18n-god-name.test.cjs:76-84`). CI runs only `npm ci`, `npm run typecheck` and `npm run check:links` (`.github/workflows/ci.yml`), so those tests fail only on a contributor's machine. Business mode adds roughly 150-250 new strings across onboarding, the team picker, agent setup, approvals, the connector center, the schedule, the close dialog and the plan-limit banner.
- **Decision:**
  - Every new key lands in `en`, `zh-CN` and `ar` in the same pull request.
  - New screens are checked in Arabic for right-to-left layout.
  - **CI gains `npm run test:focused`**, so the existing suite and everything this plan adds gate merges.
  - Machine translations for money and approval wording get a native-speaker review before the chamber pilot, since Chinese-speaking members are likely in that audience.
- **Tests:** the existing parity tests become the gate; no new test type is needed.

**12. New machinery goes in injected modules; `index.ts` only wires** (P2, confidence 9/10)

- **Finding:** `src/main/index.ts` is 5,407 lines with 153 IPC handlers, and `hive.ts` is 3,494. This plan adds at least seven new pieces: approvals executor, office schedule state, plan-limit state, pack loader and validator, connection capability map, OAuth flow, and tray plus close dialog. The repo already proves the better pattern twice: `control.ts` and `integrationBroker.ts` take injected dependencies, and the broker is "DEPENDENCY-FREE of electron by design… unit-testable under plain node" (`integrationBroker.ts:15-16`).
- **Decision:**
  - Each new piece is its own `src/main` module with dependencies injected and no Electron import: `approvals.ts`, `officeState.ts`, `planLimit.ts`, `packs.ts`, `capabilityMap.ts`, `oauth/`, `tray.ts`.
  - `index.ts` gains IPC wiring only. **No new business logic in `index.ts`.**
  - Every new module ships with unit tests, which now run in CI (Decision 11).
- **Tests:** per-module unit tests running under plain `node --test`, no app launch required.

**13. Pack and hire validators share field helpers, with strict mode for packs** (P2, confidence 9/10)

- **Finding:** `validateHireManifest` (`src/shared/hire.ts:170`) is deliberately lenient. It rebuilds its result field by field, so unrecognized fields silently disappear (`:316`). Decision 7 needs the opposite for packs, because a pack grants access. The validator also holds reusable pieces: the capped-string helper (`:150-167`), provider and model checks (`:187-198`), and the default-deny flag allowlist (`:200-242`).
- **Decision:**
  - Per-field helpers move into one shared module used by both validators: capped strings, enum checks, regex checks, bounded arrays, and the default-deny flag allowlist.
  - **Packs validate strict:** an unknown key or an invalid capability level is an error (Decision 7).
  - **Hire stays lenient**, so existing hire links keep importing, and the `munder-difflin/hire@1` spec string stays accepted alongside `dontbemichael/office-pack@1`.
  - The shared module's API makes the strictness mode explicit at the call site.
- **Tests:** both validators, covering unknown fields (dropped for hire, rejected for pack), invalid levels, and an existing hire link still importing unchanged.

**14. Every new component is specified in `DESIGN.md` in the PR that builds it** (P2, confidence 9/10)

- **Finding:** `DESIGN.md` §7 documents ten components and states that "any new component must derive from these tokens". Business mode adds eight it does not cover: the tray icon and menu, the three-way close dialog, the `Off | Ask me | On its own` control, the team-picker card with checkbox and "needs" chips, the approvals row, the connector row with connection states, the office-hours editor, and the plan-limit banner. The wireframe drew several of them, but no states are specified.
- **Decision:** each pattern gets a `DESIGN.md` entry (anatomy, states including hover, disabled, focus and error, and props) in the same pull request that implements it, derived from the existing tokens. One visual system covers both developer and business mode. The approved wireframe (`docs/designs/business-mode-office-packs-wireframe.png`) is the reference for the first four.
- **Applies a standing rule** from 2026-07-26: a UI pattern the plan needs and `DESIGN.md` lacks is added to `DESIGN.md` as part of that PR, never deferred.

**15. Five shipped texts are corrected, and prompt text gets a test** (P2, confidence 9/10)

- **Finding:** four comments and one live instruction contradict the system.
  - `hive.ts:958-959` says the hooks make an agent "drain its inbox on Stop"; `hooks.ts:261-264` explicitly refuses to.
  - `closingTime.ts:19-21` and `:72-73` both rest on that same Stop-hook drain.
  - `hiddenClaude.ts:17-19` justifies avoiding `claude -p` with a credit pool "from 2026-06-15"; Anthropic's support page says that change is paused and third-party usage still draws on the subscription.
  - `resources/skills/capabilities/SKILL.md` §3 tells agents to call the loopback broker but never names `MD_BROKER_URL` or `MD_BROKER_TOKEN`, which `index.ts:4736-4737` injects.
  - **Live behavior:** Michael's injected prompt says "You are otherwise fully autonomous — there is NO separate approval queue" (`hive.ts:1487`). Once Decision 10 ships, that text steers him away from the approvals flow.
- **Decision:** each correction rides the PR that changes the matching behavior. The business-mode Michael prompt describes the approvals queue, the capabilities skill names the broker environment variables, and the four stale comments are corrected.
- **Tests:** prompt text is treated as behavior — a test asserts the business-mode prompt contains the approvals instruction and the developer-mode prompt does not.

**16. The technical / non-technical split is removed; the product is for business owners** (P1, confidence 10/10, founder decision)

- **Finding:** onboarding's first step asks technical or non-technical and stores `audience` (`src/main/config.ts:178-182`), which only selects a copy register; Settings exposes it as a simple-mode toggle. Hanging permission mode, connector isolation and tool authorization off that flag would make a wording toggle change a security boundary.
- **Decision:**
  - **Remove the concept.** No `audience` key, no persona step, no simple-mode toggle.
  - **Onboarding starts with the business type.** The product presents itself as an app for non-technical business owners, full stop.
  - What earlier decisions called "business mode" is simply how the product behaves: the `dontAsk` permission model (Decision 4), inherited connectors off (Decision 8), central connections with per-agent authorization (Decision 9).
  - The `descPlain` copy keys become the only copy register, which also shrinks the i18n surface from Decision 11.
- **Consequences:**
  - Approach A's "hide developer surfaces behind a mode flag" becomes a product-wide question (Decision 17).
  - Divergence from upstream grows, so the sync question in Open Question 9 gets sharper.
- **Tests:** no reference to `audience` remains; onboarding's first screen is the business type; the leak test runs against normal navigation rather than a mode.

**17. Developer screens move to a hidden advanced area** (P1, confidence 9/10)

- **Finding:** removing the mode flag (Decision 16) leaves the developer surfaces without a home: terminal panes (`App.tsx:512`), the Monaco IDE and git panels (`App.tsx:513`, `ide/`, `components/git/`), the CI and GitHub view, the skills browser, the raw spawn command, cwd and worktree fields in `AddAgentModal.tsx:736-874`, and the Prerequisites list in Settings.
- **Decision:**
  - These screens move behind an **explicit unlock** in Settings (or a keyboard shortcut). They never appear in onboarding or on any normal path an owner can reach.
  - The code stays in the fork, so debugging a member's office is still possible and upstream changes to those areas can still be merged.
  - The leak test asserts that no developer term (`terminal`, `worktree`, `git`, `repo`, `CLI`, a model id) appears anywhere reachable without the unlock.
- **Tests:** normal navigation leak test; the unlock reveals the advanced area and survives a restart only if the owner re-enters it.

### Tests

**18. Backfill tests for existing modules as each is touched** (P2, confidence 9/10)

- **Finding:** the plan rests on code with no coverage today. `integrationBroker.ts` has no test at all, and Decision 2 makes it the enforcement point. `control.test.cjs` covers pauses and steers but not `toolDecision` or `gatedTools`. `hiddenClaude.ts`, `OnboardingWizard.tsx` and mission arming/firing (beyond the pure helpers in `weekly-schedule.test.cjs`) have none. The humanQA answer path that Decision 10 extends is covered only for the ledger and ordering (`task-ledger`, `askme-order`).
- **Decision:**
  - A pull request that touches one of those modules lands tests for the existing behavior first, then makes its change.
  - New modules ship with unit tests from the start (Decision 12), running under plain `node --test`.
  - Coverage today across the areas this plan touches: 5 of 31 (16%).
- **Mandatory regression test (no approval needed):** Decision 4 changes what `ensureClaudePermissionsAccepted` writes, and six existing cases in `test/claude-permissions-config.test.cjs` assert today's behavior. A regression test must prove the business build writes no skip-permission flags to `~/.claude/settings.json` while the developer path is unchanged.
- **Full coverage map and the QA-facing plan:** `~/.gstack/projects/agentvivekkumar-dontbemichael/moblizeit-main-eng-review-test-plan-20260915-233401.md`, which `/qa` and `/qa-only` read directly.

### Performance

**19. Owner agents start with no default MCP servers** (P2, confidence 8/10)

- **Finding:** six servers ship on by default (`mcpCatalog.ts`, `defaultEnabled: true` × 6) and `buildDefaultMcpServers` writes that set into every agent session (`hive.ts:1224-1241`). Stdio servers are per session, so a five-agent office starts roughly 30 extra `npx`/`uvx` processes. Each one duplicates something the agent already has:
  - `fetch` → built-in **WebFetch**; `filesystem` → built-in **Read/Write/Edit/Grep/Glob**; `time` → the bundled `temporal` skill, already copied into every agent at spawn;
  - `sequential-thinking` → a reasoning scratchpad from before models thought in steps natively;
  - `context7` (library docs) and `git` (repo inspection) → developer-only.
  - Three of the six run through `uvx`, which needs Python's `uv`, so they may silently fail to resolve on an owner's machine anyway.
- **Removal is safe, verified:**
  - nothing in `src/`, `resources/` or `test/` references `munder-filesystem`, `munder-git`, `munder-time`, `munder-fetch`, `munder-context7` or `munder-sequential-thinking`;
  - an empty set is already a supported state: "Omitted entirely when empty so a settings file with no enabled servers is unchanged from before" (`hive.ts:1170-1172`);
  - the only consumers are UI lists (`McpDefaultsSettings.tsx`, `AddAgentModal.tsx:590-593`) and the hire allowlist (`hire.ts:286-307`), none of which require a server to be enabled;
  - the only test mention is an i18n key name (`arabic-ui.test.cjs:213`).
- **Decision:** owner agents start with no default MCP servers. Their abilities are the built-in tools granted by their rules (Decision 4), the bundled skills, and the connections the owner authorized (Decision 9). The catalog stays available in the advanced area (Decision 17) for developer agents.
- **Follow-on:** `capabilities/SKILL.md:99` promises "MCP integrations (filesystem, git, and others) come pre-wired", so it is corrected under Decision 15. Any owner agent that needs to read a web page must be granted WebFetch explicitly.
- **Tests:** a business agent's session declares only its authorized connections; an empty server set writes a valid settings file.

**20. Essential infrastructure is always on; only owner-facing capabilities are per-agent** (P2, confidence 9/10, founder principle)

- **Principle:** anything the system cannot run without is not restricted per agent. It is always present and tweakable only in the advanced or settings area. Per-agent authorization (Decision 9) governs owner-facing capabilities: connections and what an agent may do with them.
- **The essential set, from the actual injection points:**
  - the hook shim and its socket, `HIVE_SOCK` (`hive.ts:832`, `:963`) — the entire event plane;
  - the per-session settings file itself (hooks, status line, sandbox write dirs);
  - bundled skills copied at every spawn (`index.ts:2767`, `hive.ts:724`): `capabilities`, `temporal`, `md-hive-sync`;
  - the loopback broker address and per-agent token (`index.ts:4736-4737`);
  - semantic memory (MemPalace) and the Knowledge Graph CLI (`knowledge.ts:83` provides `KG_ROOT`, `KG_CLI`, `KG_CORE`; injected into the prompt at `hive.ts:956`);
  - the Slack reply helper config (`index.ts:5303`).
- **Tests:** an agent with zero authorized connections still boots with its hooks, skills and memory intact.

**21. Polling cadence follows office state** (P2, confidence 9/10)

- **Finding:** the window polls every agent's inbox every 4 s plus four other timers (`useHive.ts:591-952`), and main runs the fleet snapshot every 8 s, the breaker every 30 s, worker wake every 15 s and the router every 1.5 s. Decision 1 keeps the window alive in the tray, background throttling is off (`index.ts:2310`), and `powerSaveBlocker` keeps the machine awake (`index.ts:639-648`). A restaurant closed from 10pm to 9am polls for eleven hours for work that cannot arrive.
- **Decision:**
  - While the office is paused, closed, or on a plan-limit break, the inbox poll and worker wake back off to roughly 60 s, and the router idles unless an outbox actually holds mail.
  - **Keep-awake is released when the office is closed**, so the machine sleeps overnight.
  - Normal cadence resumes at opening, with the staggered restart from Decision 6.
  - The UI states that work arriving during closed hours is picked up within about a minute.
- **Tests:** cadence per office state; keep-awake released and re-acquired at the window edges; a message arriving during closed hours is delivered at the next backed-off tick.

### From the outside voice

Local `gpt-oss:20b` reviewed the full plan and raised 24 items. Each was checked against the document and the code; **6 survived**, and 14 were rejected as contradicted by the plan (approvals idempotency, plan-limit spike ordering, UX validation, `safeStorage` availability checks, code signing, battery handling, free-versus-pro gating, PKCE details) or already open questions (Windows sandbox, upstream sync).

**22. Approval execution is serialized, and state is re-checked before commit** (P2, confidence 8/10)

- **Finding:** the file layer is already safe — registry, cursor and inbox writes are atomic (`hive.ts:756`, `:1544`), and the task ledger has a merge backstop for competing writers (`taskLedger.ts:53`). What no decision covered is ordering across the new pieces. Two approvals can execute at once against one connection while the lunch pause starts, Michael patches the same card as the ledger's scribe, and a capability-level change lands mid-flight. Every write succeeds; the sequence is undefined.
- **Failure scenario:** an approval still shows as pending for an email that was already sent, so the owner sends it twice.
- **Decision:**
  - All approval execution and card writing flows through **one queue in the approvals module**, with at most **one in-flight execution per connection**.
  - Office state and the agent's capability level are read when execution starts and **re-checked before the result is committed**, so a pause or a revoked level cannot land mid-flight.
  - Card patches go through the existing `patchTaskInLedger` path.
  - Per-connection serialization is also where third-party rate-limit backoff belongs (surviving outside finding 4).
- **Tests:** concurrent approvals on one connection execute in order; a pause starting mid-execution is observed at commit; a level revoked mid-flight stops the commit.

**23. Every owner-side record carries a schema version and upgrades on read** (P2, confidence 8/10)

- **Finding:** this plan puts four new kinds of state on owners' machines — Office Packs, the connections registry, Agent Definition v2 records and approval cards — on top of `config.json`, `registry.json`, `roster.json`, `tasks.json` and `integration-secrets.json`. Owners auto-update, so builds will read each other's files in both directions. The plan names `dontbemichael/office-pack@1` but never says what `@2` does, and the one precedent in the code is unforgiving: the hire validator matches its spec string exactly and rejects anything else (`hire.ts:177-179`).
- **Precedent to follow:** the SQLite side already does this properly — ordered, append-only `PRAGMA user_version` migrations where "migration N runs when user_version < N+1, then bumps it. NEVER edit a shipped migration — only append" (`src/main/db.ts:12-15`, `:32`, `:49`, `:91-93`). The JSON records adopt the same discipline.
- **Decision:**
  - Packs, connection records, agent definitions and approval cards each carry a schema version.
  - The app **upgrades older records in place when it reads them**, through a small function per version step, and writes the upgrade back atomically so a crash mid-upgrade leaves the original intact.
  - A **newer** record on an older build is refused with a plain "update the app to open this" message rather than a parse error.
  - Migrations are append-only and never edited once shipped.
- **Tests:** one test per migration step; a newer-version record produces the update message; an interrupted upgrade leaves the old file readable.

**24. Business mode records a minimum Claude Code version and probes for the features it needs** (P2, confidence 8/10)

- **Finding:** Decision 4 depends on `dontAsk` mode plus compiled allow and deny rules, and Decision 5 depends on the `StopFailure` `rate_limit` matcher and the `quota_auto_resume_*` notifications. Claude Code pins features to versions routinely: the Manual alias needs v2.1.200 or later, strict MCP config needs v2.1.246, and one `StopFailure` matcher needs v2.1.267. Owners install and update Claude on their own schedule. The app today checks presence, not capability, and deliberately avoids version probes because running `--version` per provider "would be a dozen process launches on every panel open" (`index.ts:3592`).
- **Failure scenario:** on an older Claude the permission model silently degrades to whatever that version does, which is the fail-open case Decision 4 exists to remove, and nobody notices until an agent does something the owner set to Off.
- **Decision:**
  - Record a **minimum Claude Code version** for business mode and check it when the office starts, with a plain message and an "Update Claude" action when it is too old.
  - Run a **capability probe** that confirms behavior rather than a number: `dontAsk` denies a tool that is not in the allowlist, and the quota hook fires in a rehearsal. Cache the result per version string so later starts cost nothing.
  - Spikes S2 and S3 produce the expected values for the probe.
  - If a probe fails, offer to **continue with reduced autonomy** (agents need approval for more actions) rather than blocking startup outright.
- **Tests:** version comparison including pre-release strings; the probe result is cached per version; below the floor, the message appears and no business agent spawns with bypass permissions.

**25. One "Needs your attention" strip carries every state the owner must act on** (P2, confidence 8/10)

- **Finding:** failures reach Michael as an inform message (`index.ts:500-512`), and the only thing that reaches the owner today is a breaker notification (`index.ts:1178-1183`). The approval event the app already emits has no listener (`hooks.ts:380-382`). This plan adds six more states an owner must notice: an agent paused waiting for an account, a connection needing reconnection, the office on a plan-limit break, a failed capability probe, a pack that would not load, and a record refused by a version check.
- **Decision:**
  - Phase 1 ships a minimal strip on the floor showing **paused agents** and the **office-on-break** state.
  - Every later decision adds its state to this strip instead of inventing its own alert. The rule: **anything that needs the owner appears here.**
  - It reuses the approvals inbox rendering but stays visually distinct, so the two never read as one list.
- **Tests:** one per state; a state that clears removes its row; the strip and the approvals inbox never show the same item twice.

**26. Main-process integration tests now; a UI driver later** (P2, confidence 9/10)

- **Finding:** seven flows in the coverage map need end-to-end proof, and there is no harness: the suite is `node --test test/*.test.cjs`, there is no Playwright or Cypress config, and exactly one Electron test exists (`test/quit-sweep.electron.test.cjs`). The success criteria ("three owners from install to a first running agent in under 20 minutes") are end-to-end claims.
- **Decision:** approvals, office state, plan limits, packs and connection authorization are driven end to end through the injected modules from Decision 12, inside the existing `node --test` suite that CI now runs. No new framework. A UI-level driver for onboarding clicks and the close dialog stays a TODO until those screens settle.
- **Tests:** five integration flows; the remaining two stay manual until the UI driver exists.

## Required outputs

### NOT in scope

- **The hive-core extraction (Approach B).** Deferred: the schema, broker and pack format built here are what a later extraction would lift out.
- **OAuth broker internals per provider beyond Google, Microsoft and Meta.** Intuit's client model is spike S4; the rest wait for the Assignment to name real needs.
- **Auto-posting to Instagram or Facebook.** Meta requires a server-side app secret and a project-run server was ruled out, so Ryan stays in draft mode until the owner supplies their own Meta app.
- **A UI-level end-to-end driver.** Deferred with Decision 26 until the screens settle.
- **Windows sandboxing for agent subprocesses.** Still Open Question 8; the platform provides none today.
- **Scaling past a single owner's machine.** The wedge is solo owners; multi-machine or hosted operation is out.
- **Pro plan features inside the app.** Pro is a website service; nothing gates on it in the product.
- **Removing the developer screens.** Decision 17 hides them instead, to keep upstream merges possible.

### What already exists

| Existing | How the plan uses it |
|---|---|
| `validateHireManifest` + deep-link/file import (`hire.ts:170`, `index.ts:2182-2255`) | Office Pack validation and import, via shared field helpers (Decision 13) |
| `control.ts` `toolDecision` + PreToolUse deny (`hooks.ts:274-287`) | Second enforcement layer under a default-deny allowlist (Decision 4) |
| `integrationBroker.ts` loopback broker + `safeStorage` secret store | The enforcement point for capabilities and the executor for approvals (Decisions 2, 10) |
| Blocked task cards with `humanQA` + the ASK ME board | The approvals queue itself, rather than a new store (Decision 10) |
| `weeklySchedule.ts`, `syncMissions`, `closingTime.ts` | Office hours and the graceful close path (Decisions 1, 6) |
| `atomicWriteJson`, `mergeTaskLedger` | Write safety under the serialized executor (Decision 22) |
| `db.ts` `PRAGMA user_version` migrations | The model the JSON records copy for versioning (Decision 23) |
| `__POSTHOG_KEY__` build-time injection (`electron.vite.config.ts:15-19`) | How provider client IDs reach official builds without entering the repo (Decision 8) |
| `terminalPool.ts` buffering unmounted terminals | Why hiding terminals is safe for delivery |
| i18n parity tests, `cast.ts`, `powerSaveBlocker`, `breaker.ts` | Locales, the roster, keep-awake and runaway protection |

**Rebuilt rather than reused, deliberately:** nothing. Every new module either has no equivalent (packs, approvals executor, office state, plan limit, OAuth, capability map) or extends an existing one.

### Failure modes

| New codepath | Realistic production failure | Test | Error handling | Owner sees |
|---|---|---|---|---|
| Approvals executor | Provider returns 401 after the connection was revoked hours earlier | ✅ Decision 10 | ✅ terminal state + inbox message | ✅ "Reconnect and approve again" |
| Approvals executor | Two approvals execute at once on one connection | ✅ Decision 22 | ✅ serialized per connection | n/a |
| Broker capability check | Provider adds an endpoint the capability map doesn't cover | ✅ Decision 2 | ✅ default deny | ⚠️ denial reaches the agent; reaches the owner only through the attention strip |
| Plan-limit detection | Claude renames the quota hook | ✅ fixtures (S3) | ✅ capability probe (Decision 24) | ✅ reduced-autonomy message |
| Office schedule | DST change shifts the pause window | ✅ Decision 6 | ✅ computed from clock | n/a |
| Pack loader | Imported pack carries a future schema version | ✅ Decision 23 | ✅ refuse with update message | ✅ "update the app to open this" |
| Tray / close | Windows tears down while "Minimize" was chosen | ✅ Decision 1 | ✅ win32 teardown guarded | ✅ office keeps running |
| Delivery | Renderer dies while the office is minimized | ⚠️ partially (main fallback tested) | ✅ main wake fallback incl. Michael | ⚠️ silent until the owner reopens |

**Critical gaps (no test AND no handling AND silent):** none remain. Two are marked ⚠️ for owner visibility and are covered by the attention strip (Decision 25).

### Diagrams

```
APPROVAL PATH (Decisions 3, 10, 22)

  agent ──calls──► broker ──capability check──► level?
                                                 │
                    ┌────────────────────────────┼───────────────┐
                    ▼ auto                       ▼ ask           ▼ off
              execute now                  blocked card      deny + reason
                    │                       (tasks.json)          │
                    ▼                            │                ▼
              result ──► agent inbox             ▼          agent told, strip
                                        owner: approve / edit / reject
                                                 │
                                    serialized executor (1 per connection)
                                                 │
                                   re-check office state + level at commit
                                                 │
                        ┌────────────────────────┼────────────────────┐
                        ▼ executed               ▼ failed             ▼ expired
                  card + inbox             retry once, then      card + inbox
                                            card + inbox
```

```
ENFORCEMENT LAYERS (Decisions 2, 4, 9)

  1. broker capability check      ← credentials live here; cannot be bypassed from a shell
  2. per-agent allow/deny rules   ← dontAsk: anything unlisted is denied
  3. PreToolUse default-deny hook ← blocks widening from the owner's personal allow rules
  4. prompt text                  ← reinforcement only, never the boundary
```

**Inline diagram candidates:** `approvals.ts` (the state machine above), `officeState.ts` (working → paused → closed → on-break transitions), `capabilityMap.ts` (request → capability → level resolution).

### Worktree parallelization

| Step | Modules touched | Depends on |
|---|---|---|
| T2, T3, T4 enforcement core | `src/shared`, `src/main` (hooks, hive, broker) | — |
| T5 approvals | `src/main/approvals.ts`, hive, renderer command center | T2, T4 |
| T6 packs | `src/shared` | T2 (shared field helpers) |
| T7, T8 shell and onboarding | `src/renderer` | T6 for the team picker's data |
| T1, T9, T10, T13 platform | `src/main/index.ts` and siblings | — |
| T14..T19 hygiene | mixed, mostly `test/`, `DESIGN.md`, i18n | — |

- **Lane A:** T2 → T4 → T5 (sequential, shared `src/main` enforcement files)
- **Lane B:** T6 → T7 → T8 (sequential, packs then the screens that render them)
- **Lane C:** T1 → T9 → T10 → T13 (sequential, all touch `index.ts`)
- **Lane D:** T14, T15, T16, T17 (independent hygiene, parallel among themselves)

**Execution order:** launch A, B, C and D in parallel worktrees. **Conflict flag:** lanes A and C both touch `src/main/index.ts` wiring; keep C's edits to the beats and tray sections, and merge A first. T18 and T19 (tests) follow their lanes rather than running alone.

## Implementation Tasks

Synthesized from this review's findings. Each task derives from a specific decision above.

- [ ] **T1 (P1, human: ~1d / CC: ~30min)** — tray — Close dialog with minimize, graceful and force paths, plus Michael in the main wake fallback (Decision 1)
- [ ] **T2 (P1, human: ~2d / CC: ~1h)** — agent-definition — Agent Definition v2 schema, enforcement compiler, default-deny hook (Decisions 2, 4, 9)
- [ ] **T3 (P1, human: ~1.5d / CC: ~45min)** — permissions — `dontAsk` spawns, no global skip-flag writes, regression test (Decision 4, spike S2)
- [ ] **T4 (P1, human: ~3d / CC: ~1.5h)** — broker — Capability map, per-agent grants, live levels, default-deny on unmapped endpoints (Decisions 2, 9)
- [ ] **T5 (P1, human: ~3d / CC: ~1.5h)** — approvals — Blocked-card approvals with serialized executor, terminal states, recovery (Decisions 3, 10, 22)
- [ ] **T6 (P1, human: ~2d / CC: ~1h)** — packs — Office Pack schema, core pack, strict validator, shared helpers, import cap (Decisions 7, 13)
- [ ] **T7 (P1, human: ~3d / CC: ~1.5h)** — onboarding — Business type, starter team, per-agent setup, failure table (Decisions 3, 16)
- [ ] **T8 (P1, human: ~2d / CC: ~1h)** — shell — Remove audience, advanced unlock for developer screens, leak test (Decisions 16, 17)
- [ ] **T9 (P2, human: ~2d / CC: ~1h)** — schedule — Soft pause from the clock, cadence by state, keep-awake release (Decisions 6, 21)
- [ ] **T10 (P2, human: ~2d / CC: ~1h)** — plan-limit — Office break via `StopFailure` and quota hooks, breaker suppression (Decision 5, spike S3)
- [ ] **T11 (P2, human: ~1.5d / CC: ~45min)** — attention — Needs-your-attention strip (Decision 25)
- [ ] **T12 (P2, human: ~1.5d / CC: ~45min)** — migrations — Schema versions with upgrade-on-read (Decision 23)
- [ ] **T13 (P2, human: ~1d / CC: ~30min)** — compat — Version floor plus cached capability probe (Decision 24)
- [ ] **T14 (P2, human: ~0.5d / CC: ~20min)** — mcp — No default MCP servers for owner agents (Decision 19)
- [ ] **T15 (P2, human: ~2d / CC: ~1h)** — i18n — Three locales for all new strings; CI runs the test suite (Decision 11)
- [ ] **T16 (P2, human: ~1d / CC: ~30min)** — design-system — Specify eight components in `DESIGN.md` (Decision 14)
- [ ] **T17 (P2, human: ~0.5d / CC: ~20min)** — docs — Correct five stale texts, add the prompt-text test (Decision 15)
- [ ] **T18 (P2, human: ~2d / CC: ~1h)** — tests — Main-process integration tests for five flows (Decision 26)
- [ ] **T19 (P2, human: ~3d / CC: ~1.5h)** — tests — Backfill tests for the six untested modules as touched (Decision 18)
- [ ] **T20 (P2, human: ~4d / CC: ~2h)** — oauth — OAuth broker, generic `oauth2` template, BYO client id, per-provider policy (Decision 8)
- [ ] **T21 (P3, human: ~1d / CC: ~30min)** — connectors — First-mailbox labeling and a read-only guard on the connector path (Decision 8)

## Design Review Decisions

From /plan-design-review on 2026-09-16. Each decision was approved individually. Initial design rating: 5/10.

### Pass 1 — Information Architecture (4/10)

**27. The owner's floor layout is defined** (P1)

- **Finding:** `DESIGN.md` §15.1 defines today's layout as the floor canvas, a 360px right panel holding a terminal view and command bar, and a bottom strip of agent cards. Decision 17 moved terminals behind the advanced unlock, leaving that panel undefined, while five new surfaces needed homes: the attention strip, approvals inbox, connector center, office schedule and plain-language agent cards. Nothing said what an owner sees when they open the app at 9am.
- **Decision — read order on the owner's floor:**
  1. **Attention strip** across the top, shown only when something needs the owner (Decision 25).
  2. **Approvals inbox** in the 360px right panel, taking the slot the terminal view used to hold.
  3. **Floor canvas** in the centre, as the ambient view.
  4. **Agent cards** along the bottom, each describing its job in plain language.
- The **connector center** and **office schedule** live in Settings, not on the floor.
- **Morning-open state:** when anything needs the owner, the attention strip is the first thing they see; otherwise the floor, with overnight activity summarized on the agent cards.
- **`DESIGN.md` §15 gains an "owner floor" template** beside the existing one, carried by task T16.

**28. Setup has one progress counter, not two** (P2)

- **Finding:** the approved wireframe puts two different counters in the same title-bar slot: screen 1 reads "step 1 of 4" (wizard steps) and screen 3 reads "2 of 4" (agents to set up). Same position, same format, different meanings, and the second denominator moves with how many agents the owner picked.
- **Decision:** setup is a **single numbered sequence**, where each picked agent is its own step. The title bar reads "Step 4 of 7 · Set up Pam", and the step dots match that same total.
  - The total is computed once the team is picked, so choosing fewer agents visibly shortens setup.
  - Screens 1 and 2 show dots without a number, because the total isn't knowable yet.
  - One counter, one meaning, everywhere in the flow.

### Pass 2 — Interaction State Coverage (5/10)

**29. The first-run floor shows what happens next** (P1)

- **Finding:** setup ends and drops the owner onto the floor at the one moment when nothing has happened: no approvals, no activity, agents idle, attention strip empty by design. The plan specifies the busy floor in detail and never its first thirty seconds, which is exactly when an owner who just connected their inbox asks whether anything is actually working.
- **Decision — every empty surface names the next event rather than reporting absence:**
  - **Approvals panel, first run:** "Nothing needs you yet. Ryan drafts tomorrow's special at 9am."
  - **Agent cards:** each shows its first scheduled action with a time, so the floor reads as a promise with a timestamp.
  - **Other empty cases follow the same rule:** no agents picked, everything done for the day, office closed overnight. Each names the next event.
- **Requirement this creates:** every agent declares a first action at setup. A reactive agent such as Kelly states the trigger instead of a time ("Kelly replies when a catering enquiry arrives").

**30. A connection row names its state in words, not colour** (P1)

- **Finding:** after Decision 8 a connection has five conditions, and two of them look alike but behave differently: the Claude-account connection can read email but never send. `DESIGN.md` §17 requires status to be carried by colour **plus** icon **plus** position, never colour alone.
- **Failure scenario:** an owner sees green beside Email, assumes Kelly can reply, and a catering enquiry sits unanswered for two days.
- **Decision — five named states, each with a distinct pixel icon and plain words:**

  | State | Row reads |
  |---|---|
  | Read-only via the owner's Claude account | "Connected · reading only (through your Claude account)" |
  | Connected by the product | "Connected" |
  | Sign-in underway | "Signing in…" |
  | Token expired or revoked | "Needs reconnecting" |
  | Never connected | "Not connected" |

- The read-only row names its limit in the same place the owner sets what an agent may do.
- All five states are specified in `DESIGN.md` alongside the connector row component (task T16).
- **Check at the minimum window width (1280px)**, since the longer rows make the connector list taller.

**31. The approvals panel has two zones and eight specified states** (P1)

- **Finding:** an approval can be pending, queued behind another on the same connection (Decision 22), retrying, executed, failed, expired, rejected or superseded (Decision 10). An owner opening the app after the lunch rush sees a mixed list, and the difference between "this went out" and "this expired unsent" is the product.
- **Failure scenario:** an expired approval reads as a sent email, nobody re-approves it, and the customer who asked about catering never hears back.
- **Decision:**
  - The panel splits into **"Needs you"** (pending, queued, retrying) and **"Done today"** (executed, failed, expired, rejected, superseded), so position answers "what still needs me" before any word is read.
  - Each state carries its own icon, words and available action. A **failed or expired** item keeps an "Approve again" button. An **executed** item shows what went out and when.
  - Finished rows clear at the next office open.
  - All eight states are specified in `DESIGN.md` with the approvals row (task T16).
- **Density check required** at 1280×800: this is the tallest surface in the app and the 360px panel needs a scroll.

**Pass 2 re-rated: 9/10.** First-run, connection and approval states are now specified end to end. It falls short of 10 because loading states during sign-in and pack import are still unstated.

### Pass 3 — User Journey and Emotional Arc (4/10)

**32. The office announces its first success, and reports each morning** (P1)

- **Finding:** the arc runs anxious (handing over an inbox) → curious (a draft appears) → small risk (approving it) → and then the moment everything depends on, learning that it worked, which the plan never marks. Decision 10 writes the result to an agent inbox and a task card; Decision 31 adds a "Done today" row. Both are records, not moments.
- **Decision:**
  - **First success:** the first time an approval executes, the floor shows a one-time confirmation tying action to outcome — "Ryan posted your special. That's the first thing your office did for you." It fires once per install and never repeats.
  - **Morning report:** the first open of each day leads with what happened while the owner was away — "Overnight: Pam sorted 14 emails and flagged 2 for you" — so value is visible before any work is requested.
  - Both follow §14's voice rules: the agent's name, never "the agent", friendly and brief.

**Journey storyboard** (required artifact, rendered from the decisions above):

| Step | Owner does | Owner feels | Plan specifies |
|---|---|---|---|
| 1 | Installs, opens the app | Curious, slightly wary | Business type first; no folder, engine or model questions (Decision 16) |
| 2 | Picks business type | Recognized ("this is for me") | Six types, each a bundled pack; "Something Else" never dead-ends (Decision 3) |
| 3 | Reviews the suggested team | Interested, weighing | Michael always on, Oscar in every pack, needs-chips show setup cost (Decisions 2, 7) |
| 4 | Sets what each agent may do | Cautious, in control | Off / Ask me / On its own per capability; rules are editable defaults (Decision 4) |
| 5 | Connects an account | **Most anxious moment** | Issue 33 below |
| 6 | Lands on the floor | Expectant, unsure it works | Empty surfaces name the next event with a time (Decision 29) |
| 7 | Sees the first draft waiting | Curious, mildly protective | Approvals panel, "Needs you" zone (Decision 31) |
| 8 | Approves it | Small risk, hopeful | Broker executes the stored request exactly once (Decisions 10, 22) |
| 9 | Learns it worked | **Relief, first trust** | One-time first-success confirmation (this decision) |
| 10 | Opens the app next morning | Trust or doubt | Morning report of overnight work (this decision) |
| 11 | Week two, raises a level | Confident delegation | Levels are owner-editable at any time (Decision 9) |

Time horizons: the **first 5 seconds** are carried by step 2 (a screen about their business, not the software), the **first 5 minutes** by steps 3-6, and the **long relationship** by steps 9-11, where the office proves it works unattended.

**33. Permissions are set before the account is connected** (P1)

- **Finding:** the per-agent screen ordered itself connect → job → limits, so the owner granted access to the inbox their restaurant runs on before seeing what the agent could do with it. It also contradicted Decision 8, which requests a scope only when a capability sits above Off: at connect time the app does not yet know the levels, so it would have to request scopes it cannot justify.
- **Decision — the screen order becomes:**
  1. **What this agent does** (the editable does / won't rules).
  2. **What this agent may use** (the Off / Ask me / On its own levels).
  3. **Connect the account**, requesting exactly the scopes those levels need.
- The connect button carries a one-line summary of what is about to be granted: "Pam will be able to read and label your mail. She can't send unless you turn that on."
- **Consequence:** pack defaults must be good enough to review before connecting, and the approved wireframe's screen 3 changes shape.

**Pass 3 re-rated: 9/10.** The arc now has its anxious moment handled with informed consent, its payoff moment, and a recurring reason to return. It stops short of 10 because the storyboard's week-two step (raising a level after trust builds) has no designed prompt.

### Pass 4 — AI Slop Risk (6/10)

Classification: **APP UI (operate)**. App UI rules apply: calm surface hierarchy, dense but readable, utility language, cards only when the card is the interaction. **No hard-rejection criteria hit.** The outside voice agreed on the classification and found no hard rejections.

**34. Business-type tiles use pixel glyphs, not emoji** (P2)

- **Finding:** the first screen an owner ever sees used emoji as its only iconography (🍜 🛍 📋 ✂ 🔧 ?). `DESIGN.md` §14 states "Never: Emojis in copy. We have icons", §10 defines the pixel icon system, and "emoji as design elements" sits on the AI-slop blacklist. Emoji also render in each platform's own full-colour style, so a pixel-art cream interface receives glossy vendor artwork at whatever size the font picks, breaking both the ≤8-colour screen limit and the 4px grid.
- **Decision:** each business type gets a **16px two-colour pixel glyph** in the §10 style — bowl, shopfront, folder, scissors, wrench, question mark — snapped to the grid and drawn from the existing palette. The six glyphs are specified in `DESIGN.md` with the tile component (task T16).
- **Consequence:** every future Office Pack ships a glyph, so the pack schema gains an icon reference.

**35. The close dialog names each choice by its consequence** (P2)

- **Finding:** the three choices read "Minimize", "Close all agents gracefully" and "Force kill all". Graceful shutdown and force kill are process vocabulary; §14 sets the opposite register ("Ada hit a snag", never "An error has occurred"). This is also the one dialog where a wrong click destroys work in progress.
- **Decision — the three choices become:**
  - **"Keep the office running"** — minimize to the tray.
  - **"Send everyone home"** — finish current work, then quit.
  - **"Stop everything now"** — quit immediately; unfinished work is lost.
- The destructive choice sits last and uses the destructive button variant (§7.2). The same words carry into the tray menu, and the dialog is specified in `DESIGN.md` (task T16).
- **Translation note:** Chinese and Arabic wording must keep the consequence explicit, even where the office metaphor doesn't carry.

**Pass 4 re-rated: 9/10.** Classification is APP UI, no hard rejections, and the two slop-adjacent findings (platform emoji, process vocabulary) are resolved. Short of 10 because motion is still unspecified for the new components, which Pass 6 covers.

### Pass 5 — Design System Alignment (5/10)

**36. One readable floor at 14px, and `DESIGN.md`'s contradiction is resolved** (P1)

- **Finding:** §17 sets a hard floor — "never go below 14 px for any user-facing text" — while §4.1 defines `display-sm` at 8px for "Badges, chip labels", and the approved wireframe runs chips at 11px and secondary text at 12px. The system disagrees with itself and the new screens took the smaller side, on chips that carry meaning ("needs: Email", "starts in draft mode", "Connected · reading only") for an audience older than a developer tool's.
- **Decision:**
  - Every owner-facing string sits at **14px or above**. Chips move from 8px Press Start 2P to **14px Pixelify Sans**; secondary text goes from 12px to 14px.
  - **§4.1 is amended:** `display-sm` is marked decorative-only and never used for load-bearing text, removing the conflict with §17 permanently (task T16).
  - **Density check at 1280×800**, since taller chips reflow the team-picker and connector rows.

**37. The thirteen new states reuse the existing status palette** (P2)

- **Finding:** §3.4 defines ten status colours and every one describes an agent's lifecycle. This review added two families the palette has never carried: five connection states (Decision 30) and eight approval outcomes (Decision 31). Without a mapping, the first surface built picks a green and the next picks a different one.
- **Decision — map by meaning onto existing tokens, adding none:**

  | New state | Token |
  |---|---|
  | Needs reconnecting, failed | `status-blocked` (coral) |
  | Executed | `status-success` (mint) |
  | Pending, queued | `status-waiting` (blue) |
  | Signing in, retrying | `status-thinking` (teal) |
  | Not connected, expired | `status-idle` (grey) |
  | Connected, connected read-only | `status-success` with the read-only wording carrying the limit |
  | Rejected, superseded | `status-ghost` |

- The ≤8-colours-per-screen limit holds, and "needs you" reads identically on the connector row, the approvals panel and the attention strip.
- The mapping is written into `DESIGN.md` §3.4 as a second table (task T16).
- **Accepted approximation:** an expired approval and a never-connected account share grey, and rely on icon and words to differ, which §17 already requires.

**Pass 5 re-rated: 9/10.** Type floor resolved, palette extended by mapping rather than addition, and every new component now has a home in `DESIGN.md`. Short of 10 until those component specs are actually written (task T16).

### Pass 6 — Responsive and Accessibility (3/10)

**38. The owner floor has a specified narrow layout, and approvals keep their slot** (P1)

- **Finding:** §15.1 sets a 1280×800 minimum and collapses the 360px right panel to a bottom drawer below 1024px. Decision 27 put the approvals inbox in that panel and Decision 31 made it the tallest surface in the app, so on a 13-inch laptop the thing owners open the app to check could become a drawer. Nothing stated what the attention strip, the taller 14px chips or the office-hours strip do as the window narrows.
- **Decision — behaviour by width:**
  - **At 1280px:** the right panel holds its 360px and the floor canvas absorbs the loss. The attention strip becomes a single line with a count. Chips wrap rather than truncate. The office-hours strip drops to two lines.
  - **Below 1024px:** the **floor canvas hides entirely** rather than the panel collapsing, so approvals survive the narrowest case.
  - This inverts §15.1's existing collapse for the owner layout, so the developer layout and the owner layout now collapse differently and both need checking.
- Written into `DESIGN.md` §15 alongside the owner-floor template (task T16). Gives the density checks from Decisions 30, 31 and 36 a defined target.

**39. Each new component states its keyboard contract, and the close dialog is safe by default** (P1)

- **Finding:** §17 promises Tab reachability with the §7.2 focus outline and reduced-motion support, but none of the eight new components said how. Two cases change outcomes rather than convenience: the three-way tool control would cost three Tab stops per capability (fifteen on Pam's screen alone), and nothing said which button the close dialog focuses or what Escape does, so Escape could land on the destructive choice.
- **Decision:**
  - **Tool level control** behaves as a radio group: one Tab stop per capability, arrow keys to change. Pam's five capabilities cost five Tab stops, not fifteen.
  - **Close dialog** opens with "Keep the office running" focused, and **Escape maps to that same safe choice**, never to "Stop everything now".
  - **Approval rows** are one Tab stop with their actions reachable from it; **attention-strip rows** are focusable; the **tray menu** follows OS conventions.
  - Focus order per screen and reduced-motion behaviour are specified in `DESIGN.md` with each component (task T16).

**Pass 6 re-rated: 9/10.** Narrow-width behaviour, keyboard semantics and destructive-dialog safety are now specified. Short of 10 until touch-target sizes are confirmed, which only matters if the office ever reaches a tablet.

### Pass 7 — Unresolved Design Decisions

**40. Every new component carries a §12 motion token; first success is the authored moment** (P2)

- **Finding:** none of the eight new components stated how they move, so each would either animate to its author's taste or sit inert. §12 already fixes the vocabulary (200ms snap-in for modals and drawers, instant hover and press, 200ms toast slide) and §12.2 forbids springs, bounce, parallax and ambient idle motion on UI panels.
- **Decision:**
  - **Close dialog and tray menu:** the 200ms modal snap-in.
  - **Plan-limit banner:** slides like a toast, 200ms.
  - **Attention-strip rows:** snap in at 200ms.
  - **Approval rows:** appear instantly; only the move into "Done today" cross-fades at 200ms.
  - **First-success confirmation (Decision 32):** gets the §12.3 sparkle already defined for task completion. This is the **single authored moment** on the owner's floor, and it never fires on routine approvals.
  - Under `prefers-reduced-motion`, all of the above become instant.

**Pass 4 re-rated: 10/10.** Motion is now specified from the existing vocabulary, which was the last slop-adjacent gap.

**41. Loading states, and an offer to raise a level once trust is earned** (P2)

- **Finding:** two gaps named by earlier re-ratings. Signing in opens a browser and returns seconds later, and pack import reads and validates a file, but neither said what the owner sees meanwhile, so both can read as a dead click. Separately, Decision 9 lets owners raise a capability from "Ask me" to "On its own", but nothing ever suggests it, so most owners would approve the same routine draft forever.
- **Decision:**
  - **Sign-in:** the button becomes "Signing in…" and disables while the browser is open, with a way back if the owner abandons the flow.
  - **Pack import:** shows a validating state before the review screen.
  - **Trust prompt:** after an agent's routine action has been approved unchanged **five times**, the attention strip offers, once per capability: "Ryan's specials have been approved 5 times. Let Ryan post these on his own?" with **Yes** and **Not yet**. It never asks twice for the same capability.
- **Accepted uncertainty:** five is a guess until real owners use it; the chamber sessions are where that number gets checked.

**Pass 1 re-rated: 10/10** (the floor's empty state was its remaining gap, closed by Decision 29). **Pass 2 re-rated: 10/10** and **Pass 3 re-rated: 10/10** (loading states and the week-two prompt were their remaining gaps, closed here).

**42. The first setup screen names the product** (P2, from the outside voice)

- **Finding:** the local design pass answered NO to "is the brand unmistakable in the first screen", and the wireframe confirms it: the first-run dialog reads "Set Up Your Office" with no product name or mark anywhere. §15.1's app title bar belongs to the main view, and onboarding is a fixed overlay covering it, so setup runs with no product identity at all. A chamber member who installs after a workshop and opens the app days later has nothing confirming they opened the right thing.
- **Decision:** step 1 carries a small pixel wordmark above the dialog with one line of purpose — "Dont Be Michael · an office of agents that works while you run your business." Later setup steps drop it, so the brand appears exactly where the question is live and nowhere it would be chrome.
- Specified in `DESIGN.md` with the onboarding shell (task T16). **Height check at 1280×800**, since step 1 already carries the most content.

**43. The engine is named and explained once, in owner language** (P2, from the outside voice)

- **Finding:** step 1 read "You'll need Claude on this computer. A Max plan is recommended." That is a product name many owners won't know and a subscription tier they definitely won't, on the screen where Decision 16 promised they never meet the machinery. Hiding it is not available either: owners pay Anthropic directly, Decision 24 checks the version at startup, and Decision 5's banner says "Office on break until 3:10 pm (Claude plan limit)".
- **Decision:**
  - The line becomes owner language: "Your office runs on Claude, the AI from Anthropic. You'll need it on this computer — we found yours and you're signed in."
  - When Claude is **missing**, the same screen explains what it is, what it costs and what a Max plan buys, before sending the owner to install it.
  - The plan-limit banner reuses the name the owner already met, so its first appearance is never an error.

### Design Review — Completion Summary

| Pass | Before | After |
|---|---:|---:|
| 1 Information Architecture | 4/10 | 10/10 |
| 2 Interaction States | 5/10 | 10/10 |
| 3 User Journey | 4/10 | 10/10 |
| 4 AI Slop Risk | 6/10 | 10/10 |
| 5 Design System Alignment | 5/10 | 9/10 |
| 6 Responsive and Accessibility | 3/10 | 9/10 |
| 7 Decision register | — | 4 resolved, 0 deferred |
| **Overall** | **5/10** | **9/10** |

- **17 decisions** added (27-43). **0 unresolved.**
- Passes 5 and 6 stop at 9 deliberately: the `DESIGN.md` component specs are written under task T16 rather than in this review, and touch-target sizes stay unconfirmed until the office reaches a tablet.
- **Mockups:** none generated. The design binary returned `API error (401): Incorrect API key provided` on a stale key in `~/.gstack/openai.json`; re-auth with `design/dist/design setup`. The approved wireframe at `docs/designs/business-mode-office-packs-wireframe.png` remains the visual reference, with screens 1 and 3 now changed by Decisions 28, 33, 34, 42 and 43.
- **Outside voice:** local ollama `gpt-oss:20b`, 9 findings raised, 3 survived verification (no brand on the first screen, the unexplained engine name, and unspecified motion). The other 6 were rejected, five of them for misreading §14's 12-word rule, which applies to system feedback rather than to body copy.

## Folder & Knowledge Decisions

From the owner working through onboarding on 2026-09-22/23, after research into how the harness folder, per-agent workspaces and the Memory & Knowledge store are actually used. Each decision was made by the owner.

**What the research found**

- **Agents already use their hive workspace as a document store.** `hive/agents/<id>/` is defined in `PROTOCOL.md` as plumbing (`identity.md`, `memory.md`, `inbox/`, `outbox/`), but on the owner's install it holds 16 PDFs and spreadsheets in Oscar's workspace, 43 drafts in Ryan's and a 3,789-file project in Sadiq's. The hive root has also grown `shared/`, `templates/` and `social/` folders the agents made up. Nobody designed this. The workspace was the only place an agent was told was its own.
- **Those files are committed.** The hive is a git repo the app commits to automatically (11,147 commits, 90 MB `.git`), and it ignores only named plumbing files. 32 PDFs are now in its history.
- **Michael runs inside the harness folder.** `useHive.ts` spawns him with `cwd: config.harnessHome`, so everything he writes lands among the plumbing. Workers already run in a folder the owner picks, labelled "Project" in the hire modal.
- **The owner had already built the target design by hand:** workers pointed at `~/Documents/munder-difflin/moblizeit/{sales,support,marketing,hr,IT,finance,admin}`.
- **The app writes nothing into an agent's working folder.** Skills are copied into the agent's hive directory, and every other access to `cwd` is a read. Claude Code itself leaves one empty `.claude/.cc-writes/` directory there.
- **The harness folder cannot be synced.** It holds a git repo committed constantly, the Unix socket agents' permission hooks connect through (`hive/hooks.sock`, which fails open when unreachable), and a SQLite memory store (`palace/chroma.sqlite3`).
- **Memory & Knowledge could barely read an owner's files.** Word, Excel and PowerPoint were indexed as zip bytes and reported as a successful upload. PDFs needed a `pdftotext` binary a typical owner's Mac lacks. Images get no OCR. It is off by default, and when on, every agent is told to search it.

**44. Every agent works inside its own folder** (P1)

- An agent's folder is its **working directory**: the context it reads and the work it produces both live there, so drafts land in it without the agent having to remember.
- **Several agents can share a folder.**
- **Michael works in a shared "Office" folder**, which every agent can also reach. He leaves the harness folder.

**45. Folders default to `~/Documents/<Business name>/<Folder>`** (P1)

- The owner can instead point any folder at an existing location anywhere, including Dropbox or Google Drive. Documents may sync; only the harness folder must not.
- The app creates a default folder only if it is missing, and never overwrites an existing one.

**46. Agents can read and write their folders** (P2)

- Always read-write, by the owner's choice. An agent's edit or deletion in a synced folder syncs everywhere; the cloud providers' version history is the recovery path.

**47. Folders are suggested per agent during onboarding** (P1)

- Each pack names a folder for each agent (Oscar → Finance, Ryan → Marketing). On the pick-your-team screen the owner can accept it, point it elsewhere, or skip it.

**48. The harness folder is plumbing only: instruct and block** (P1)

- Agents are told where their work belongs, and the PreToolUse hook refuses writes into the harness folder outside plumbing paths.
- **Caveat:** that hook currently fails open when it cannot reach `hooks.sock`, so the block is not airtight until the fail-open is fixed. Writes made through a shell command cannot be blocked this way either.

**49. Existing files stay where they are** (P3)

- Business files already in the hive are neither moved nor purged from its history. The rules apply from now on.

**50. Memory & Knowledge is the company-wide store, and it reads real documents** (P1) — *built*

- It stays "what every agent should know", alongside per-agent folders.
- Word, Excel and PowerPoint are read from their XML (`fflate`), PDFs by a bundled PDF.js (`unpdf`), and `.doc`, `.rtf` and `.odt` through macOS's built-in `textutil`. Implemented in `src/main/docText.ts`, with 16 tests on real files.
- A file that cannot be read (a scan, Pages/Numbers/Keynote, a password-protected file) is **refused with a plain reason** that names the file, instead of being stored as junk and reported as added.
- **Scans and photos (F7):** read with the OCR built into macOS (the Vision framework behind Live Text, rendered page by page through PDFKit), reached via the built-in `osascript`. Nothing to bundle or download. First use loads Vision's models once (~30 s); a warm page takes under a second. Up to 50 pages per scan. On other platforms a scan is refused with a reason.

**51. Everything ships inside the DMG** (P1)

- No Homebrew, no system tools the owner must install. Verified by packaging the real `.app` and reading a Word file and a PDF through the libraries inside its `app.asar`, under the app's own Electron 32 / Node 20.

**52. Every character keeps the same job in every business type** (P1)

- The owner's rule: jobs follow The Office. Oscar is finance, Dwight sales, Kelly customer support, Pam executive admin, Ryan marketing, Sadiq IT security, Toby HR, Nick IT engineering, Creed quality control, Meredith supply chain, Darryl inventory and shipping. Michael is the office manager on every team.
- A business type chooses **which** characters are on its team and how each job reads for that trade. It never changes **what** the job is, and the folder follows the job.
- Held in code by `src/shared/officeRoles.ts` and a fixture test over every shipped pack. Angela (records) and Stanley (scheduling) left the packs: that work is executive admin, so it moved to Pam.
- Nick, Sadiq and Darryl had no sprite. Each was added as a portrait recipe and rendered against the existing cast to check they read as distinct people.

**53. Meet your office comes before the team** (P2)
- Order: business, Meet your office, team, home, manager, permissions. The owner first learns that Michael is the office manager who runs the floor, then picks the team he will run. Picking a team before knowing who manages it read backwards.
- The "Suggest my team" button moved with it: it now sits on Meet your office, the screen that leads into team selection. Step 1 says Next.

**54. This build runs on Claude Code only, and says a Max plan is needed** (P1)
- Setup step 5 shows one engine, Claude Code, and the owner picks the Claude model (Opus 5.5 recommended, Decision 55). `BUILD_ENGINES` in `src/shared/agentProvider.ts` holds the list. Every other engine preset stays in the code, so bringing one back means adding its id there.
- The same screen says a Claude Max plan is needed to keep the office running full time, with a link to Anthropic's plans. Smaller plans hit their usage limit during the day and the office pauses until it resets.
- Setup only, for now. The Add agent and Edit agent windows and the Command Center model picker still list every engine.

**55. Michael runs on Opus 5.5, and this repo owns the model list** (P1)
- The recommended manager model was Opus 4.8 (1M), set upstream on 2026-06-21 when it was the newest and never updated. No commit, doc or test gave a reason to keep it. Anthropic's docs make Opus 5.5 the default on every paid plan, with a native 1M window. It is now the recommendation, the default, and first in the Claude list.
- The pickers refresh their list every 6 hours from a file on GitHub, and that file replaces the built in list. It was upstream's file, so another project chose our owners' models (it had no Opus 5.5). It is now `docs/model-catalog.json` in this repo, and a cached copy from any other address is ignored. **It goes live only once that file is pushed to this repo's main.** Until then the fetch finds the older file on main, which has no Opus 5.5.
- Opus 5.5 needs Claude Code 2.1.280+. Every Claude agent spawn reads the installed version (cached per binary, refreshed when it updates) and runs Opus 5 on an older CLI, logged. An unknown version keeps the model. Rules in `src/shared/modelCliFloor.ts`.

**56. Michael's panel is for an owner, not a developer** (P1)
- ASK ME is Michael's first tab and where his panel opens; the terminal is second. Focus mode still opens on the terminal, because reaching it means asking for it.
- Hidden in this build, code kept, each behind a switch in `src/shared/buildFeatures.ts`: git (the agents' GIT tab and the IDE's git rail, `SHOW_GIT`), the IDE (`SHOW_IDE`; a file link in a terminal shows the file in Finder), and temporary workers (`ALLOW_TEMP_WORKERS`).
- SKILLS moved from Michael's panel to Settings: a catalog skill installs to `~/.claude/skills` and bundled skills are copied into every agent, so they were never Michael's.
- MONITOR and ACTIVITY became collapsible cards in one ADVANCED tab, last, in the TRIGGERS style. Links to either open ADVANCED on that card.
- **No temporary workers.** Michael could start helpers himself (spawn requests), a spend the owner never saw. The queue, his instructions, the protocol section, the write permission, the Settings switch and the WORKERS tab are all closed. Instead, when a job is outside every team member's role and big enough to pull Michael off the floor, he adds an ASK ME card: add a team member (naming the cast member whose fixed job fits), hand it to the closest one, let Michael do it once, or drop it, with a recommendation. Small jobs he still just does or routes.

**57. The app is "Don't Be Michael" and nothing points upstream** (P1)
- Name everywhere people see it: menu, window, packaged app, installers (`Dont-Be-Michael-…`), macOS permission prompts, on screen text. Bundle id `io.github.agentvivekkumar.dontbemichael`. The data folder is `dontbemichael`, pinned before the rename so a later rename never moves it. It was `munder-difflin` in 0.0.1; the owner chose a clean folder over carrying that data forward (2026-09-24).
- Updates, the Settings hero payload, the model list and every GitHub link use `agentvivekkumar/dontbemichael`. The URL scheme is `dontbemichael://`, so the upstream website's hire links no longer open this app.
- Removed: upstream's Pro announcement, Founders' Wall offer, Discord links, blog links, funding page, and its maintainer's security email.
- Kept on purpose: README credit for the fork, CHANGELOG / CONTRIBUTORS history, and internal ids (hire file format `munder-difflin/hire@1`, pipe names, user agents). `test/no-upstream.test.cjs` scans the app, build and GitHub config.

**Superseded:** Decision 28 said screens 1 and 2 carry dots but no step number, because the total isn't known until the team is picked. At the owner's request every screen now shows "Step N of 6", and `test/onboarding-steps.test.cjs` keeps the numbers in sync with the flow. The total will change again when the pick-your-team screen lands.

**Build order**

| # | Piece | Size | Status |
|---|---|---|---|
| F1 | Packs name a folder per agent (schema, validator, shipped packs) | S | done: `folder` on agent definitions, all 26 shipped agents |
| F2 | Folder paths: safe names, `~/Documents/<Business>/<Folder>`, create if missing | S | done: `src/main/agentFolders.ts` |
| F3 | Pick-your-team screen (wireframe screen 2), each agent showing its folder | L | done: step 3 of onboarding, after Meet your office (Decision 53); replaces "Your projects"; saves `businessTeam` + `officeFolder` and creates the folders |
| F4 | Michael and every agent run in their folder; everyone reaches Office | M | done: Michael runs in Office; the picked team starts once, each in its folder, with its pack job as a standing goal; Office writable by all |
| F5 | Harness is plumbing only: instructions plus the write block | M | done: prompt line + PreToolUse refusal (`src/main/harnessGuard.ts`); business installs only; fails open like every hook, and shell writes can't be judged |
| F6 | Agents can read Word/Excel in their own folders (the same converter, as a tool) | M | done: `doc-text` CLI built as a second main entry; verified running from inside the packaged app's asar |
| F7 | OCR for scans and photos | M | done: `src/main/macOcr.ts` — macOS Vision + PDFKit via `osascript`, zero install; scanned PDFs, photos, and the agents' `doc-text` tool |

## Design Review — Required Outputs

### NOT in scope

- **Touch targets and tablet layout.** The office runs on a desktop; revisit if it ever reaches a tablet.
- **Dark mode.** `DESIGN.md` §18 already defers it; cream is the brand.
- **Custom sprite art for the business cast.** Portraits stay programmatic (`portraitArt.ts`), as §18 records.
- **Sound.** §13 is spec-only and off by default; none of the new moments use it.
- **A visual identity refresh.** The pixel system stands; this review specified inside it.
- **Regenerated AI mockups.** The generator returned a 401 on a stale key, so the approved wireframe remains the visual reference.

### What already exists

| Existing | How the new screens use it |
|---|---|
| `PixelPanel` dialog variant (§7.1) | The onboarding shell and the close dialog |
| `PixelBadge` (§7.3) | Needs-chips, connection states, approval states |
| `AgentCard` (§7.4) | Base for the team-picker row and the floor's agent cards |
| `ConfigDrawer` (§7.9) | Closest existing pattern for per-agent setup |
| `Toast` (§7.7) | The plan-limit banner's motion and shape |
| `PixelButton` destructive variant (§7.2) | "Stop everything now" in the close dialog |
| §3.4 status palette | All thirteen new states, by mapping rather than addition |
| §12 motion tokens | Every new component's transitions |
| §12.3 sparkle | The first-success moment |
| The approved wireframe | The visual reference for onboarding screens 1-3 |

**Six of the eight "new" components are variants of existing ones.** Only the tray menu and the office-hours editor are genuinely new shapes.

### Implementation Tasks

Synthesized from this review's findings. Each derives from a specific decision above.

- [ ] **T1 (P1, human: ~2d / CC: ~1h)** — owner-floor — Owner floor layout and its narrow-width behaviour (Decisions 27, 38)
- [ ] **T2 (P1, human: ~1.5d / CC: ~45min)** — onboarding — Single progress counter; per-agent screen reordered to job → limits → connect (Decisions 28, 33)
- [ ] **T3 (P1, human: ~1.5d / CC: ~45min)** — states — First-run and empty states naming the next event; sign-in and import loading states (Decisions 29, 41)
- [ ] **T4 (P1, human: ~1d / CC: ~30min)** — connectors — Connector row with five named states (Decision 30)
- [ ] **T5 (P1, human: ~1.5d / CC: ~45min)** — approvals — Two-zone approvals panel with eight states (Decision 31)
- [ ] **T6 (P2, human: ~1.5d / CC: ~30min)** — moments — First-success confirmation and morning report (Decision 32)
- [ ] **T7 (P2, human: ~2h / CC: ~30min)** — copy-and-icons — Six pixel glyphs; close dialog worded by consequence (Decisions 34, 35)
- [ ] **T8 (P2, human: ~1.5d / CC: ~30min)** — design-system — 14px floor; reconcile §4.1 with §17; add the §3.4 mapping table (Decisions 36, 37)
- [ ] **T9 (P2, human: ~1.5d / CC: ~30min)** — a11y-motion — Keyboard contracts and motion tokens, with reduced-motion fallbacks (Decisions 39, 40)
- [ ] **T10 (P3, human: ~1d / CC: ~20min)** — trust-prompt — Offer to raise a capability after five unchanged approvals (Decision 41)

All ten fold into eng-review task **T16** (`DESIGN.md` specs) and the Phase 1 UI tasks, rather than forming a separate workstream.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Outside Review | ollama `gpt-oss:20b`, plan + design phases | Independent 2nd opinion | 2 | completed | 33 raised, 9 survived verification |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | clean | 25 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | clean | score 5/10 → 9/10, 17 decisions |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**OUTSIDE COVERAGE:** local ollama `gpt-oss:20b` in both phases, on 2026-09-16.

- **plan-review phase:** 24 items raised, 6 survived verification. Three became Decisions 22, 23 and 24 (serialized executor, schema versioning, version floor plus capability probe), one became Decision 25 (attention strip), and two were captured in `TODOS.md` (rate-limit backoff, `safeStorage` unavailable path). The other 14 were contradicted by the plan or already open questions.
- **design phase:** 9 items raised, 3 survived verification — no brand on the first screen (Decision 42), the unexplained engine name (Decision 43), and unspecified motion (Decision 40). Five of the six rejections came from one misreading: §14's 12-word limit applies to system feedback, not body copy.

**CROSS-MODEL:** in both phases the outside voice and the native review overlapped on nothing. Its verified hit rate was 6 of 24 on the plan and 3 of 9 on the design, around 25% each, consistent with prior passes on this machine. Its value was cross-cutting concerns the section-by-section passes were not looking for: ordering between components, state migration across app updates, engine version drift, and absent brand identity.

**VERDICT:** ENG + DESIGN CLEARED — ready to implement. CEO and DX reviews were not run and do not gate. The eng review predates the 17 design decisions, though none of them moved a module boundary, a schema or an enforcement point.

NO UNRESOLVED DECISIONS
